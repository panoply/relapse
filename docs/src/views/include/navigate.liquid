{% for link in collections.all %}
  {% if link.url == page.url %}
    <hr>
    <div class="row jc-between mt-1 navigate">
      <div class="col-auto">
        <a
          href="{{ link.data.prev.uri }}"
          spx-position="y:0"
          class="btn pl-0">
          <span class="mr-2">
            {% svg 'chevron-left', 'icon icon-left' %}
          </span>
          {{ link.data.prev.label }}
        </a>
      </div>
      <div class="col-auto">
        <a
          href="{{ link.data.next.uri }}"
          spx-position="y:0"
          class="btn pr-0">
          {{ link.data.next.label }}
          <span class="ml-2">
            {% svg 'chevron-right', 'icon icon-right' %}
          </span>
        </a>
      </div>
    </div>
  {% endif %}
{% endfor %}