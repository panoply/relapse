(function () {
  'use strict';

  var __async = (__this, __arguments, generator) => {
    return new Promise((resolve, reject) => {
      var fulfilled = (value) => {
        try {
          step(generator.next(value));
        } catch (e2) {
          reject(e2);
        }
      };
      var rejected = (value) => {
        try {
          step(generator.throw(value));
        } catch (e2) {
          reject(e2);
        }
      };
      var step = (x2) => x2.done ? resolve(x2.value) : Promise.resolve(x2.value).then(fulfilled, rejected);
      step((generator = generator.apply(__this, __arguments)).next());
    });
  };

  // ../../../brixtol/packages/spx/index.js
  var hr = Object.defineProperty;
  var yr = (e2, t2) => {
    for (var o in t2)
      hr(e2, o, { get: t2[o], enumerable: true });
  };
  var D = typeof window != "undefined" ? window : { screen: {}, navigator: {} };
  var be = (D.matchMedia || function() {
    return { matches: false };
  }).bind(D);
  var xr = { get passive() {
    return true;
  } };
  var to = function() {
  };
  D.addEventListener && D.addEventListener("p", to, xr);
  D.removeEventListener && D.removeEventListener("p", to, false);
  var vt = "ontouchstart" in D;
  var vr = "TouchEvent" in D;
  var Tt = vt || vr && be("(any-pointer: coarse)").matches;
  var eo = (D.navigator.maxTouchPoints || 0) > 0 || Tt;
  var oo = D.navigator.userAgent || "";
  var Tr = be("(pointer: coarse)").matches && /iPad|Macintosh/.test(oo) && Math.min(D.screen.width || 0, D.screen.height || 0) >= 768;
  var br = (be("(pointer: coarse)").matches || !be("(pointer: fine)").matches && vt) && !/Windows.*Firefox/.test(oo);
  var Ir = be("(any-pointer: fine)").matches || be("(any-hover: hover)").matches || Tr || !vt;
  var Me = eo && (Ir || !br) ? "hybrid" : eo ? "touchOnly" : "mouseOnly";
  var bt = typeof window != "undefined";
  "content" in document.createElement("template");
  document.createRange && "createContextualFragment" in document.createRange();
  var q = Tt ? "pointer" : "mouse";
  var le = window.location.origin;
  var { assign: $, keys: En, entries: xn } = Object;
  var ee = Object.defineProperty;
  var X = Object.defineProperties;
  var ro = Object.create;
  var { isArray: M } = Array;
  var no = Array.from;
  var x = "";
  var { warn: It, info: so, error: At } = console;
  var A = () => document.body;
  var io = () => document.head;
  var m = (e2) => e2 ? $(ro(null), e2) : ro(null);
  var B = (e2) => new Set(e2);
  var ao = (e2) => new Proxy(m(), e2);
  var K = () => /* @__PURE__ */ new Map();
  var E = class extends XMLHttpRequest {
    constructor() {
      super(...arguments);
      this.key = null;
    }
  };
  E.o = K(), E.a = K(), E.r = m();
  var n = m({ index: "", eval: true, patched: false, loaded: false, logLevel: 2, qs: m(), config: m({ fragments: ["body"], timeout: 3e4, globalThis: true, schema: "spx-", manual: false, logLevel: 2, cache: true, components: null, maxCache: 100, reverse: true, preload: null, annotate: false, eval: m({ script: null, style: null, link: null, meta: false }), hover: m({ trigger: "href", threshold: 250 }), intersect: m({ rootMargin: "0px 0px 0px 0px", threshold: 0 }), proximity: m({ distance: 75, threshold: 250, throttle: 500 }), progress: m({ bgColor: "#111", barHeight: "3px", minimum: 0.08, easing: "linear", speed: 200, threshold: 500, trickle: true, trickleSpeed: 200 }) }), fragments: K(), components: m({ registry: K(), instances: K(), connected: B(), elements: K(), reference: ao({ get: (e2, t2) => n.components.instances.get(e2[t2]) }) }), events: m(), observe: m(), memory: m(), pages: m(), snaps: m(), resources: B() });
  function p(e2, t2, o) {
    let r = "\x1B[96mSPX\x1B[0m ", s = n.config.logLevel;
    if (M(t2) && (t2 = t2.join(" ")), e2 === 1 && s === 1 || e2 === 2 && (s === 1 || s === 2))
      so(`${r}%c${t2}`, `color: ${o || "#999"};`);
    else if (e2 === 3 && s < 4)
      o ? It(r + t2, o) : It(r + t2);
    else if (e2 === 5 || e2 === 4) {
      o ? At(r + t2, o) : At(r + t2);
      try {
        throw e2 === 4 ? new TypeError(t2) : new Error(t2);
      } catch (i2) {
      }
    }
  }
  var co = /\b(?:append|prepend)/;
  var fo = /\s+/g;
  var Se = /^\b(?:true|false)$/i;
  var Ge = /^\d*\.?\d+$/;
  var qe = /^(?:[.-]?\d*\.?\d+|NaN)$/;
  var lo = /\b(?:intersect|hover|proximity)\b/;
  var po = /\b(?:SCRIPT|STYLE|LINK)\b/;
  var mo = /\[(['"]?.*['"]?,?)\]/;
  var Rt = /[xy]\s*|\d*\.?\d+/gi;
  function go(e2) {
    let t2 = e2.replace(/\s+,/g, ",").replace(/,\s+/g, ",").replace(/['"]/g, x);
    return t2.charCodeAt(0) === 91 && (/^\[\s*\[/.test(t2) || /,/.test(t2) && /\]$/.test(t2)) && (t2 = t2.replace(/^\[/, x).replace(/\]$/, x)), t2.split(/,|\|/);
  }
  function Ie(e2, t2) {
    try {
      let o = (t2 || e2).replace(/\\'|'/g, (r) => r[0] === "\\" ? r : '"').replace(/([{,])\s*(.+?)\s*:/g, '$1 "$2":');
      return JSON.parse(o);
    } catch (o) {
      return p(5, "Invalid JSON expression in attribute value: " + JSON.stringify(e2 || t2, null, 2), o), t2;
    }
  }
  function Ae(e2) {
    return e2[e2.length - 1];
  }
  function Ar(e2) {
    return e2.replace(/\s+/g, " ").trim();
  }
  function ho(e2) {
    return e2.replace(/\./g, "\\.").replace(/@/g, "\\@").replace(/:/g, "\\:");
  }
  function Je(e2) {
    return Ar(e2.replace(/[\s .]+/g, ".")).replace(/\s+/g, " ").trim().split(/[ ,]/);
  }
  function yo(e2) {
    if (qe.test(e2))
      return e2 === "NaN" ? NaN : +e2;
    if (Se.test(e2))
      return e2 === "true";
    let t2 = e2.charCodeAt(0);
    return t2 === 123 || t2 === 91 ? Ie(e2) : e2;
  }
  function Eo() {
    return new Promise((e2) => setTimeout(() => e2(), 1));
  }
  function H(e2) {
    setTimeout(() => e2(), 1);
  }
  function xo() {
    return Promise.resolve();
  }
  function vo(e2) {
    let t2 = document.createElement("textarea");
    return t2.innerHTML = e2, t2.value;
  }
  function Ze() {
    return (/* @__PURE__ */ new Date()).getTime();
  }
  function Re(e2) {
    return (t2) => t2 ? typeof t2 == "string" ? t2 in e2 : t2.every((o) => o in e2) : false;
  }
  function T(e2, t2) {
    return e2 ? t2 in e2 : false;
  }
  function Qe(e2, t2, o) {
    if (arguments.length > 1)
      ee(e2, t2, { get() {
        return o;
      } });
    else
      return (r, s, i2) => {
        if (T(e2, r))
          return;
        function a2() {
          return s;
        }
        return ee(e2, r, i2 ? $(i2, { get: a2 }) : { get: a2 });
      };
  }
  function Pt(e2) {
    return T(e2, "target") ? e2.target.length === 1 && e2.target[0] === "body" ? e2.target : e2.target.filter((t2, o, r) => t2 !== "body" && t2 !== x && t2.indexOf(",") === -1 ? r.indexOf(t2) === o : false) : n.config.fragments.length === 1 && n.config.fragments[0] === "body" ? ["body"] : [];
  }
  function we(e2) {
    return e2.length === 1 && e2[0] === "body" ? "body" : e2.length === 0 ? null : e2.join(",");
  }
  function Pe(e2) {
    let t2 = typeof e2;
    if (t2 === "object") {
      for (let o in e2)
        return false;
      return true;
    } else {
      if (t2 === "string")
        return e2[0] === void 0;
      if (M(e2))
        return e2.length > 0;
    }
    return false;
  }
  function To(...e2) {
    return e2.join(x);
  }
  var W = function e(t2 = 5) {
    let o = Math.random().toString(36).slice(-t2);
    return e.g.has(o) ? e(t2) : (e.g.add(o), o);
  };
  W.g = B();
  function bo(e2 = 2) {
    return (t2, o) => {
      let r = t2.length;
      return (r < 1 || t2[r - 1].length === e2 ? t2.push([o]) : t2[r - 1].push(o)) && t2;
    };
  }
  function Io(e2) {
    return e2 < 1024 ? e2 + " B" : e2 < 1048576 ? (e2 / 1024).toFixed(1) + " KB" : e2 < 1073741824 ? (e2 / 1048576).toFixed(1) + " MB" : (e2 / 1073741824).toFixed(1) + " GB";
  }
  function Nt(e2) {
    return e2[0].toLowerCase() + e2.slice(1);
  }
  function Le(e2) {
    return e2[0].toUpperCase() + e2.slice(1);
  }
  function pe(e2) {
    return /[_-]/.test(Nt(e2)) ? e2.replace(/([_-]+).{1}/g, (t2, o) => t2[o.length].toUpperCase()) : e2;
  }
  function te(e2, t2) {
    let o = typeof e2 == "string" ? A().querySelectorAll(e2) : e2, r = o.length;
    if (r !== 0)
      for (let s = 0; s < r && t2(o[s], s) !== false; s++)
        ;
  }
  function Y(e2, t2) {
    if (arguments.length === 1)
      return (r) => Y(e2, r);
    let o = t2.length;
    if (o !== 0)
      for (let r = 0; r < o; r++)
        e2(t2[r], r, t2);
  }
  function Ct(e2) {
    for (let t2 in e2)
      delete e2[t2];
  }
  function Ro() {
    if (n.patched)
      return;
    n.patched = true;
    let e2 = Element.prototype.setAttribute, t2 = document.createElement("i");
    Element.prototype.setAttribute = function(r, s) {
      if (r.indexOf("@") < 0)
        return e2.call(this, r, s);
      t2.innerHTML = `<i ${r}="${s}"></i>`;
      let i2 = t2.firstElementChild.getAttributeNode(r);
      t2.firstElementChild.removeAttributeNode(i2), this.setAttributeNode(i2);
    };
  }
  function Rr() {
    let e2 = [], t2 = document.createElement("div"), o = null, r, s = null, i2 = ({ bgColor: h2, barHeight: b, speed: fe, easing: gr }) => {
      t2.style.cssText = To("pointer-events:none;", `background-color:${h2};`, `height:${b};`, "position:fixed;", "display:block;", "z-index:2147483647;", "top:0;", "left:0;", "width:100%;", "will-change:opacity,transform;", `transition:transform ${fe}ms ${gr};`);
    }, a2 = (h2) => (-1 + h2) * 100, c = (h2, b, fe) => h2 < b ? b : h2 > fe ? fe : h2, f2 = () => s || (t2.style.setProperty("transform", `translateX(${a2(o || 0)}%)`), s = A().appendChild(t2), t2), l2 = () => {
      let h2 = A();
      if (h2.contains(s)) {
        let b = s.animate({ opacity: ["1", "0"] }, { easing: "ease-out", duration: 100 });
        b.onfinish = () => {
          h2.removeChild(s), s = null;
        };
      } else
        s = null;
    }, u2 = () => {
      let h2 = e2.shift();
      h2 && h2(u2);
    }, d = (h2) => {
      e2.push(h2), e2.length === 1 && u2();
    }, y = (h2) => {
      h2 = c(h2, n.config.progress.minimum, 1), o = h2 === 1 ? null : h2;
      let b = f2();
      d((fe) => {
        b.style.setProperty("transform", `translateX(${a2(h2)}%)`), h2 === 1 ? setTimeout(() => {
          l2(), fe();
        }, n.config.progress.speed * 2) : setTimeout(fe, n.config.progress.speed);
      });
    }, v = (h2) => {
      let b = o;
      if (!b)
        return ce();
      if (b < 1)
        return typeof h2 != "number" && (b >= 0 && b < 0.2 ? h2 = 0.1 : b >= 0.2 && b < 0.5 ? h2 = 0.04 : b >= 0.5 && b < 0.8 ? h2 = 0.02 : b >= 0.8 && b < 0.99 ? h2 = 5e-3 : h2 = 0), b = c(b + h2, 0, 0.994), y(b);
    }, S2 = () => {
      setTimeout(() => {
        o && (v(), S2());
      }, n.config.progress.trickleSpeed);
    };
    function ce(h2) {
      n.config.progress && (r = setTimeout(() => {
        o || y(0), n.config.progress.trickle && S2();
      }, h2 || 0));
    }
    function dr(h2) {
      clearTimeout(r), !(!h2 && !o) && (v(0.3 + 0.5 * Math.random()), y(1));
    }
    return { start: ce, done: dr, style: i2 };
  }
  var me = Rr();
  function et(e2, t2) {
    let o = e2.name, r = t2;
    t2 = Nt(t2 || o), T(e2, "connect") || (e2.connect = { id: t2, state: {}, nodes: [] });
    let s = Re(e2.connect);
    return s("state") || (e2.connect.state = {}), s("nodes") || (e2.connect.nodes = []), s("id") || (e2.connect.id = t2), t2 !== e2.connect.id && (t2 = pe(e2.connect.id)), o !== r && /^[A-Z]|[_-]/.test(e2.connect.id) && p(3, [`Component identifer name "${e2.connect.id}" must use camelCase format.`, `The identifer has been converted to "${t2}"`]), t2;
  }
  function oe(e2, t2 = false) {
    let { registry: o } = n.components;
    for (let r in e2) {
      let s = e2[r], i2 = t2 ? r : et(s, r);
      o.has(i2) || (o.set(i2, s), p(1, `${s.name} component registered under: ${i2}`));
    }
    n.config.components || (n.config.components = true);
  }
  function Pr(e2) {
    for (let t2 of ["hover", "intersect", "proximity", "progress"])
      T(e2, t2) && (e2[t2] === false ? n.config[t2] = false : typeof e2[t2] == "object" && $(n.config[t2], e2[t2]), delete e2[t2]);
    return e2;
  }
  function He(e2, t2) {
    let o = `:not([${e2}${t2}=false]):not([${e2}link])`;
    switch (t2.charCodeAt(0)) {
      case 104:
        return `${o}:not([${e2}proximity]):not([${e2}intersect])`;
      case 105:
        return `${o}:not([${e2}hover]):not([${e2}proximity])`;
      case 112:
        return `${o}:not([${e2}intersect]):not([${e2}hover])`;
    }
  }
  function Nr(e2, t2, o) {
    if ("eval" in e2)
      if (e2.eval) {
        if (typeof e2.eval == "object") {
          let r = $(n.config.eval, e2.eval);
          n.eval = !(!r.link && !r.meta && !r.script && !r.style);
        }
      } else
        n.eval = false;
    return (r) => {
      if (n.eval === false || n.config.eval[r] === false)
        return `${r}[${t2}eval]:${o}`;
      if (n.config.eval[r] === true)
        return `${r}:${o}`;
      let s = r === "link" ? `${r}[rel=stylesheet]:${o}` : r === "script" ? `${r}:${o}:not([${t2}eval=hydrate])` : `${r}:${o}`;
      if (n.config.eval[r] === null)
        return s;
      if (M(n.config.eval[r]))
        return n.config.eval[r].length > 0 ? n.config.eval[r].map((i2) => `${i2}:${o}`).join(",") : (p(3, `Missing eval ${r} value, SPX will use defaults`), s);
      p(4, `Invalid eval ${r} value, expected boolean or array`);
    };
  }
  function Cr(e2) {
    let t2 = [];
    if ("fragments" in e2 && M(e2.fragments) && e2.fragments.length > 0)
      for (let o of e2.fragments) {
        let r = o.charCodeAt(0);
        if (r === 46 || r === 91) {
          p(3, [`Invalid fragment selector "${o}" provided. Fragments must be id annotated values.`, "Use spx-target attributes for additional fragment selections."]);
          continue;
        } else
          r === 35 ? t2.push(o.slice(1).trim()) : t2.push(o.trim());
      }
    return t2;
  }
  function Po(e2 = m()) {
    Ro(), X(n, { ready: { get() {
      return document.readyState === "complete";
    } }, types: { get() {
      return m({ INITIAL: 0, PREFETCH: 1, FETCH: 2, PRELOAD: 3, REVERSE: 4, POPSTATE: 5, VISIT: 6, HYDRATE: 7, CAPTURE: 8, RELOAD: 9, HOVER: 10, INTERSECT: 11, PROXIMITY: 12 });
    } } }), "components" in e2 && (oe(e2.components), delete e2.components), $(n.config, Pr(e2));
    let t2 = n.config.schema, o = t2 === "spx" ? "spx" : t2.endsWith("-") ? t2 : t2 === null ? x : `${t2}-`, r = `:not([${o}disable]):not([href^=\\#])`, s = `not([${o}eval=false])`, i2 = Nr(e2, o, s);
    n.config.fragments = Cr(e2), n.config.schema = o, n.config.index = null, n.memory.bytes = 0, n.memory.visits = 0, n.memory.limit = n.config.maxCache, $(n.qs, { v: new RegExp(`^href|${o}(hydrate|append|prepend|target|progress|threshold|scroll|position|proximity|hover|cache|history)$`, "i"), M: new RegExp(`${o}(?:node|bind|component)|@[a-z]|[a-z]:[a-z]`, "i"), T: new RegExp(`^${o}[a-zA-Z0-9-]+:`, "i"), m: `${o}target`, h: `[${o}target]:not([${o}target=false])`, c: `${o}morph`, b: `${o}eval`, I: `[${o}intersect]${He(o, "intersect")}`, S: `[${o}track]:not([${o}track=false])`, u: `${o}component`, y: `${o}node`, E: `${o}bind`, e: "data-spx", A: n.config.annotate ? `a[${o}link]${r}` : `a${r}`, R: i2("script"), P: i2("style"), N: i2("link"), C: i2("meta"), q: `script[${o}eval=hydrate]:${s}`, w: `link[rel=stylesheet][href*=\\.css]:${s},script[src*=\\.js]:${s}`, d: `${o}data:`, x: `a[${o}proximity]${r}${He(o, "proximity")}`, O: `a${r}${He(o, "intersect")}`, l: n.config.hover !== false && n.config.hover.trigger === "href" ? `a${r}${He(o, "hover")}` : `a[${o}hover]${r}${He(o, "hover")}` }), me.style(n.config.progress);
  }
  function ue(e2) {
    return new DOMParser().parseFromString(e2, "text/html");
  }
  function de(e2) {
    return (e2 || document).documentElement.outerHTML;
  }
  function $t(e2) {
    let t2 = e2.indexOf("<title");
    if (t2 === -1)
      return x;
    let o = e2.indexOf(">", t2) + 1, r = e2.indexOf("</title", o);
    return vo(e2.slice(o, r).trim());
  }
  function N(e2, ...t2) {
    let o = e2 === "before:cache";
    o && (t2[1] = ue(t2[1]));
    let r = true;
    return Y((s) => {
      let i2 = s.apply(null, t2);
      o ? i2 instanceof Document ? r = i2.documentElement.outerHTML : typeof r != "string" && (r = i2 !== false) : r = i2 !== false;
    }, n.events[e2] || []), r;
  }
  function Ot(e2, t2, o) {
    return e2 in n.events || (n.events[e2] = []), n.events[e2].push(o ? t2.bind(o) : t2) - 1;
  }
  function Mt(e2, t2) {
    if (e2 in n.events) {
      let o = n.events[e2];
      if (o && typeof t2 == "number")
        o.splice(t2, 1), p(2, `Removed ${e2} event listener (id: ${t2})`), o.length === 0 && delete n.events[e2];
      else {
        let r = [];
        if (o && t2)
          for (let s = 0, i2 = o.length; s < i2; s++)
            o[s] !== t2 ? r.push(o[s]) : p(2, `Removed ${e2} event listener (id: ${s})`);
        r.length ? n.events[e2] = r : delete n.events[e2];
      }
    } else
      p(3, `There are no ${e2} event listeners`);
    return this;
  }
  function ke(e2, { method: t2 = "GET", body: o = null, headers: r = null, type: s = "text" } = {}) {
    return new Promise(function(i2, a2) {
      let c = new E();
      if (c.key = e2, c.responseType = s, c.open(t2, e2), c.setRequestHeader("spx-request", "true"), r !== null)
        for (let f2 in r)
          c.setRequestHeader(f2, r[f2]);
      c.onloadstart = function() {
        E.o.set(this.key, c);
      }, c.onload = function() {
        i2(this.response);
      }, c.onerror = function() {
        a2(this.statusText);
      }, c.onabort = function() {
        delete E.r[this.key], E.a.delete(this.key), E.o.delete(this.key);
      }, c.onloadend = function(f2) {
        E.o.delete(this.key), n.memory.bytes = n.memory.bytes + f2.loaded, n.memory.visits = n.memory.visits + 1;
      }, c.send(o);
    });
  }
  function tt(e2) {
    return e2 in E.r ? (clearTimeout(E.r[e2]), delete E.r[e2]) : true;
  }
  function ot(e2, t2, o) {
    e2 in E.r || w(e2) || (E.r[e2] = setTimeout(t2, o));
  }
  function St(e2) {
    for (let [t2, o] of E.o)
      e2 !== t2 && (o.abort(), p(3, `Pending request aborted: ${t2}`));
  }
  function No(e2) {
    if (n.config.preload !== null) {
      if (M(n.config.preload)) {
        let t2 = n.config.preload.filter((o) => {
          let r = I(o, 3);
          return r.key !== o ? O(C(r)) : false;
        });
        return Promise.allSettled(t2);
      } else if (typeof n.config.preload == "object" && T(n.config.preload, e2.key)) {
        let t2 = n.config.preload[e2.key].map((o) => O(C(I(o, 3))));
        return Promise.allSettled(t2);
      }
    }
  }
  function rt(e2) {
    return __async(this, null, function* () {
      if (e2.rev === e2.key)
        return;
      let t2 = C(I(e2.rev, 4));
      yield Eo(), O(t2).then((o) => {
        o ? p(2, `Reverse fetch completed: ${o.rev}`) : p(3, `Reverse fetch failed: ${e2.rev}`);
      });
    });
  }
  function De(e2) {
    return __async(this, null, function* () {
      if (!E.a.has(e2.key))
        return e2;
      let t2 = yield E.a.get(e2.key);
      return E.a.delete(e2.key), delete E.r[e2.key], Ne(e2, t2);
    });
  }
  function O(e2) {
    return __async(this, null, function* () {
      return E.o.has(e2.key) && e2.type !== 7 ? (e2.type === 4 && E.o.has(e2.rev) ? (E.o.get(e2.rev).abort(), p(3, `Request aborted: ${e2.rev}`)) : p(3, `Request in transit: ${e2.key}`), false) : N("fetch", e2) ? (E.a.set(e2.key, ke(e2.key)), De(e2)) : (p(3, `Request cancelled via dispatched event: ${e2.key}`), false);
    });
  }
  function We(e2, t2) {
    if (t2(e2) === false)
      return;
    let o, r;
    for (e2.firstElementChild && (r = 0, o = e2.children[r]); o; )
      (o.nodeName === "svg" || o.nodeName === "CODE") && o.childElementCount > 0 && (r = 0, o = o.nextElementSibling), o && We(o, t2), o = e2.children[++r];
  }
  function Co(e2, t2) {
    return `on${e2}` in t2 ? true : (p(5, [`Invalid event name "${e2}" provided. No such event exists in the DOM API.`, "Only known event listeners can be attached."], t2), false);
  }
  function $o(e2, t2, o) {
    let r = e2[t2.method];
    return function(i2) {
      t2.params && (T(i2, "attrs") || Qe(i2, "attrs", m()), $(i2.attrs, t2.params)), r.call(e2, i2);
    };
  }
  function Fe(e2, t2) {
    t2.attached && (t2.listener.abort(), t2.listener = new AbortController(), t2.options.signal = t2.listener.signal, t2.attached = false, n.components.elements.delete(t2.dom), p(1, [`Detached ${t2.key} ${t2.eventName} event from the ${t2.method} method on`, `${e2.static.id} component (${e2.scope.key})`]));
  }
  function nt(e2, t2, o) {
    if (!o.attached) {
      if (!(o.method in e2)) {
        p(3, `Undefined callback method: ${e2.static.id}.${o.method}()`);
        return;
      }
      Oo(t2.attributes, o), o.isWindow ? Co(o.eventName, window) && addEventListener(o.eventName, $o(e2, o)) : Co(o.eventName, t2) && (t2.addEventListener(o.eventName, $o(e2, o), o.options), n.components.elements.set(o.dom, t2)), p(1, [`Attached ${o.key} ${o.eventName} event to the ${o.method} method on`, `${e2.static.id} component (${e2.scope.key})`]), o.attached = true;
    }
  }
  function Mo(e2) {
    H(() => st(z(n.page.key).body, e2));
  }
  function qt(e2, t2, o, r) {
    let s, i2, a2;
    for (e2.firstElementChild && (s = 0, i2 = e2.children[s], a2 = t2.children[s]); i2; )
      i2.nodeName === "svg" && i2.childElementCount > 0 && (s = 0, i2 = i2.nextElementSibling, a2 = a2.nextElementSibling), i2 && (r.has(a2) ? (st(i2, o, true), r.delete(a2)) : a2.hasAttribute(n.qs.e) && i2.setAttribute(n.qs.e, a2.getAttribute(n.qs.e)), qt(i2, a2, o, r)), i2 = e2.children[++s], a2 = t2.children[s];
  }
  function st(e2, t2, o = false) {
    let { elements: r } = n.components;
    We(e2, (s) => {
      if (re(s.attributes) && !s.hasAttribute(n.qs.e)) {
        let i2 = t2.shift();
        if (i2 || p(5, "Undefined reference, snapshot record failed to align", s), r.has(i2)) {
          let a2 = s.getAttribute(n.qs.e), f2 = r.get(i2).getAttribute(n.qs.e);
          if (a2 !== f2 && s.setAttribute(n.qs.e, f2), t2.length === 0)
            return o === false && (it(s.ownerDocument.documentElement.outerHTML), p(1, "Snapshot updated")), false;
        } else
          p(5, "Undefined reference, snapshot record failed to align", s);
      }
    });
  }
  function wt(e2, t2) {
    let { page: o, dom: r } = So(n.page.key), s = e2.charCodeAt(0) === 114 ? "removed" : "appended";
    r.head.contains(t2) ? (r.head[e2](t2), n.snaps[o.snap] = r.documentElement.outerHTML, p(1, `Snapshot record was updated. Node ${s} from <head>`, t2)) : p(3, "Node does not exists in snapshot record, no mutation applied", t2);
  }
  function $r(e2, { nodes: t2 }) {
    let o = m();
    for (let r in t2)
      t2[r].schema in o || (o[t2[r].schema] = []), o[t2[r].schema].push(t2[r].dom);
    for (let r in o) {
      let s = o[r];
      s.length > 1 ? X(e2, { [r]: { get() {
        return n.components.elements.get(s[0]);
      } }, [`${r}s`]: { get() {
        return s.map((i2) => n.components.elements.get(i2));
      } } }) : ee(e2, r, { get() {
        return n.components.elements.get(s[0]);
      } });
    }
  }
  function at({ p: e2, f: t2, n: o, c: r }) {
    n.page.type === 0 && o.length > 0 && Mo(o);
    let s = n.page.type === 4, { elements: i2, connected: a2, instances: c, registry: f2, reference: l2 } = n.components;
    for (let u2 in e2)
      for (let d of e2[u2]) {
        if (d.instanceOf === null)
          if (u2 in t2)
            d.instanceOf = t2[u2];
          else
            continue;
        let y, v;
        if (d.mounted === false && (r !== null || s)) {
          let S2 = qo();
          d.alias !== null && d.alias in S2 ? (v = S2[d.alias][0], y = v.static) : d.instanceOf in S2 && S2[d.instanceOf].length === 1 && (v = S2[d.instanceOf][0], y = v.static), d.key = v.scope.key, d.ref = v.scope.ref;
        } else
          y = f2.get(d.instanceOf), v = new y(d, y.connect);
        if ($r(v, d), r === null && "nodes" in y && y.nodes.length > 0)
          for (let S2 of y.nodes)
            ee(v, `has${Le(S2)}Node`, { get() {
              return `${S2}Node` in this;
            } });
        for (let S2 in d.events) {
          let ce;
          r !== null && d.mounted === false ? (ce = v.scope.events[S2] = d.events[S2], l2[S2] = v.scope.key) : ce = d.events[S2], nt(v, i2.get(ce.dom), ce);
        }
        (r === null || (r !== null || s) && d.mounted === true) && (a2.add(d.key), c.set(d.key, v), p(1, `Mounted (init) component ${v.static.id} (${d.key})`, "#6dd093"), "oninit" in v && v.oninit(n.page), "onload" in v && v.onload(n.page));
      }
  }
  function ct() {
    let e2 = [];
    for (let t2 of n.page.fragments) {
      let o = document.getElementById(t2);
      o ? (n.fragments.set(t2, o), e2.push(t2)) : n.fragments.delete(t2);
    }
    U("fragments", e2);
  }
  function wo(e2) {
    e2.type !== 6 && H(() => {
      let t2 = z(e2.snap), o = e2.selector !== "body" && e2.selector !== null ? `${e2.target.join()},${n.qs.h}` : n.qs.h, r = t2.body.querySelectorAll(o), s = e2.type === 0 ? A().querySelectorAll(o) : null;
      te(r, (i2, a2) => {
        if (!i2.hasAttribute("id"))
          i2.setAttribute("id", `t.${W()}`), s !== null && s[a2].setAttribute("id", `t.${W()}`);
        else if (i2.id.startsWith("t."))
          return;
        e2.fragments.push(i2.id);
      }), it(t2.documentElement.outerHTML, e2.snap);
    });
  }
  function Lt(e2) {
    for (let [t2, o] of n.fragments)
      if (t2 === e2.id || o.contains(e2))
        return true;
    return false;
  }
  function Or(e2) {
    return e2.trim().replace(/\s+/, " ").split(/[|, ]/).map(pe);
  }
  function Oo(e2, t2) {
    for (let o = 0, r = e2.length; o < r; o++) {
      let { name: s, value: i2 } = e2[o];
      if (n.qs.T.test(s) && !s.startsWith(n.qs.d) && i2) {
        let a2 = s.slice(n.config.schema.length).split(":").pop();
        t2.params === null && (t2.params = m()), a2 in t2.params || (t2.params[a2] = yo(i2));
      }
    }
  }
  function re(e2) {
    if (typeof e2 == "string")
      return e2.indexOf("@") > -1 || e2 === n.qs.u || e2 === n.qs.y || e2 === n.qs.E;
    for (let t2 = e2.length - 1; t2 >= 0; t2--)
      if (re(e2[t2].name))
        return true;
    return false;
  }
  function Ce(e2, t2) {
    re(e2.attributes) && (e2.hasAttribute(n.qs.u) ? wr(e2, e2.getAttribute(n.qs.u), t2) : Lo(e2, t2, null, null));
  }
  function lt(e2 = null) {
    return m({ f: m(), p: m(), t: null, n: [], c: e2, L: e2 ? m() : null });
  }
  function pt(e2, { p: t2, f: o }) {
    return e2 in t2 ? e2 in o ? Ae(t2[o[e2]]) : Ae(t2[e2]) : e2 in o ? Ae(t2[o[e2]]) : (t2[e2] = [Ht(e2)], t2[e2][0]);
  }
  function ne(e2, t2, o) {
    n.components.reference[o] = t2;
    let r = e2.getAttribute(n.qs.e), s = r ? `${r},${o}` : o;
    return e2.setAttribute(n.qs.e, s), o;
  }
  function Ht(e2, t2, o) {
    let { registry: r } = n.components, s = W(), i2 = m({ key: s, mounted: false, ref: `c.${s}`, state: m(), nodes: m(), events: m(), binds: m() });
    return t2 && (ne(t2, s, i2.ref), i2.dom = o.t, i2.mounted = true, i2.inFragment = Lt(t2), t2.hasAttribute("id") && (i2.alias = pe(t2.id.trim()))), r.has(e2) ? (i2.instanceOf = e2, i2.alias ? r.has(i2.alias) ? p(5, [`Component alias "${i2.alias}" matches a component identifer in the registry.`, "An alias reference must be unique and cannot match component names."]) : o.f[i2.alias] = e2 : i2.alias = null) : (i2.alias = e2 || null, i2.instanceOf = null, i2.mounted && (o.f[i2.alias] = null)), i2;
  }
  function Mr(e2, t2, o, r) {
    let s = m(), i2 = o.indexOf("{"), a2 = t2.slice(n.config.schema.length), c = new AbortController();
    s.key = `e.${W()}`, s.dom = `${r.t}`, s.isWindow = a2.startsWith("window:"), s.eventName = s.isWindow ? a2.slice(7) : a2, s.attached = false, s.params = null, s.options = { signal: c.signal };
    let f2 = o;
    if (i2 > -1) {
      let v = o.slice(i2, o.lastIndexOf("}", i2)).match(/(passive|once)/g);
      v !== null && (v.indexOf("once") > -1 && (s.options.once = true), v.indexOf("passive") > -1 && (s.options.passive = true)), f2 = o.slice(0, i2);
    }
    let l2 = Je(f2);
    l2.length > 1 && p(3, `No more than 1 DOM Event listener method allowed in value: ${o}`);
    let [u2, d] = l2[0].split("."), y = pt(u2, r);
    s.listener = c, s.method = d.trim(), y.events[s.key] = s, ne(e2, y.key, s.key);
  }
  function Sr(e2, t2, o) {
    for (let r of Je(t2)) {
      let [s, i2] = r.split("."), a2 = pt(s, o), c = ne(e2, a2.key, `n.${W()}`);
      a2.nodes[c] = m({ key: c, keyProp: i2, dom: o.t, schema: `${i2}Node`, isChild: a2.mounted });
    }
  }
  function qr(e2, t2, o) {
    for (let r of Je(t2)) {
      let [s, i2] = r.split("."), a2 = pt(s, o), c = ne(e2, a2.key, `b.${W()}`);
      i2 in a2.binds || (a2.binds[i2] = m()), a2.binds[i2][c] = m({ key: c, stateKey: i2, value: e2.innerText, dom: o.t, stateAttr: `${n.config.schema}${s}:${i2}`, selector: `[${n.qs.e}*=${ho(c)}]`, isChild: a2.mounted });
    }
  }
  function Lo(e2, t2, o, r) {
    o === null && r === null && (t2.t = W(), t2.n.push(t2.t), n.components.elements.set(t2.t, e2));
    for (let s = e2.attributes.length - 1; s >= 0; s--) {
      let { name: i2, value: a2 } = e2.attributes[s];
      if (o) {
        let c = `${n.config.schema}${o}:`;
        r && !i2.startsWith(c) && (c = `${n.config.schema}${r}:`), i2.startsWith(c) && (pt(o, t2).state[i2.slice(c.length)] = a2);
      }
      i2.indexOf("@") > -1 ? Mr(e2, i2, a2, t2) : i2 === n.qs.E ? qr(e2, a2, t2) : i2 === n.qs.y && Sr(e2, a2, t2);
    }
  }
  function wr(e2, t2, o) {
    let { registry: r, elements: s } = n.components, { p: i2, f: a2 } = o, c = e2.hasAttribute("id") ? e2.id.trim() : null;
    o.t = W(), o.n.push(o.t), s.set(o.t, e2);
    for (let f2 of Or(t2))
      if (!r.has(f2))
        p(5, `Component does not exist in registry: ${f2}`);
      else {
        let l2;
        f2 in i2 ? (l2 = Ae(i2[f2]), l2.mounted === false ? (ne(e2, l2.key, l2.ref), l2.dom = o.t, l2.mounted = true, l2.inFragment = Lt(e2)) : i2[f2].push(Ht(f2, e2, o))) : i2[f2] = [Ht(f2, e2, o)], l2 = Ae(i2[f2]), c && !(c in a2) && (a2[c] = f2), Lo(e2, o, f2, l2.alias);
      }
  }
  function mt(e2) {
    let t2 = lt();
    if (e2)
      if (e2 instanceof Set) {
        for (let o of e2)
          Ce(o, t2);
        return e2.clear(), t2;
      } else
        return Ce(e2, t2), t2;
    else {
      if (We(A(), (o) => Ce(o, t2)), Pe(t2.p))
        return;
      at(t2);
    }
  }
  var R;
  function Ho() {
    H(() => {
      R = void 0;
    });
  }
  function kt(e2, t2, o) {
    let { reference: r, connected: s, elements: i2 } = n.components;
    for (let a2 of t2) {
      let c = r[a2];
      if (!c)
        continue;
      let f2 = a2.charCodeAt(0);
      if (f2 === 99) {
        c.scope.mounted = false, s.delete(c.scope.key), i2.delete(c.scope.dom);
        let { scope: l2 } = c;
        for (let u2 in l2.nodes)
          i2.delete(l2.nodes[u2].dom);
        for (let u2 in l2.binds)
          for (let d in l2.binds[u2])
            i2.delete(l2.binds[u2][d].dom);
        for (let u2 in l2.events)
          Fe(c, l2.events[u2]);
        p(1, `Unmounted component ${c.static.id} (${l2.key})`, "#8F5150");
      } else if (f2 === 101)
        Fe(c, c.scope.events[a2]);
      else if (f2 === 110) {
        let l2 = c.scope.nodes[a2];
        o && e2.isEqualNode(o) ? (ne(e2, c.scope.key, a2), R.n.push(l2.dom), i2.set(l2.dom, o)) : i2.delete(l2.dom);
      } else if (f2 === 98) {
        let { binds: l2 } = c.scope;
        for (let u2 in l2)
          if (a2 in l2[u2]) {
            o && e2.isEqualNode(o) ? (ne(e2, c.scope.key, a2), R.n.push(l2[u2][a2].dom)) : i2.delete(l2[u2][a2].dom);
            break;
          }
      }
    }
  }
  function Dt(e2, t2) {
    let { reference: o, connected: r, elements: s } = n.components;
    for (let i2 of t2) {
      let a2 = o[i2];
      if (!a2)
        continue;
      let c = i2.charCodeAt(0);
      if (c === 99)
        r.add(a2.scope.key), s.set(a2.scope.dom, e2), a2.scope.mounted = true, p(1, `Mounted component ${a2.static.id} (${a2.scope.key})`, "#6dd093");
      else if (c === 101)
        nt(a2, e2, a2.scope.events[i2]);
      else if (c === 110)
        s.set(a2.scope.nodes[i2].dom, e2);
      else if (c === 98) {
        let { binds: f2 } = a2.scope;
        for (let l2 in f2)
          if (i2 in f2[l2]) {
            e2.innerText = f2[l2][i2].value, s.set(f2[l2][i2].dom, e2);
            break;
          }
      }
    }
  }
  function Ve(e2) {
    e2.nodeType !== 1 && e2.nodeType !== 11 || e2.hasAttribute(n.qs.e) && kt(e2, e2.getAttribute(n.qs.e).split(","));
  }
  function ko(e2) {
    e2.hasAttribute(n.qs.e) ? Dt(e2, e2.getAttribute(n.qs.e).split(",")) : re(e2.attributes) && (R ? R.c = e2 : R = lt(e2), Ce(e2, R));
  }
  function Do(e2, t2, o, r) {
    o && (o = o.split(",")), r && (r = r.split(",")), o && r ? (kt(e2, o), Dt(e2, r)) : !o && r ? Dt(e2, r) : (R ? R.c = t2 : R = lt(t2), o && !r && kt(e2, o, t2), Ce(e2, R));
  }
  function ut(e2, t2, o) {
    e2[o] !== t2[o] && (e2[o] = t2[o], e2[o] ? e2.setAttribute(o, x) : e2.removeAttribute(o));
  }
  function Fo(e2, t2) {
    if (t2.nodeType === 11 || e2.nodeType === 11)
      return;
    let o = t2.attributes, r = e2.getAttribute(n.qs.e), s = t2.getAttribute(n.qs.e), i2 = false, a2, c, f2, l2, u2;
    for (let y = o.length - 1; y >= 0; y--)
      a2 = o[y], c = a2.name, l2 = a2.value, f2 = a2.namespaceURI, f2 ? (c = a2.localName || c, u2 = e2.getAttributeNS(f2, c), u2 !== l2 && (a2.prefix === "xmlns" && (c = a2.name), e2.setAttributeNS(f2, c, l2))) : (u2 = e2.getAttribute(c), u2 !== l2 && (e2.setAttribute(c, l2), !r && !s && !i2 && (i2 = re(c))));
    let d = e2.attributes;
    for (let y = d.length - 1; y >= 0; y--)
      a2 = d[y], c = a2.name, l2 = a2.value, f2 = a2.namespaceURI, f2 ? (c = a2.localName || c, t2.hasAttributeNS(f2, c) || e2.removeAttributeNS(f2, c)) : t2.hasAttribute(c) || e2.removeAttribute(c);
    (r || s || i2) && Do(e2, t2, r, s);
  }
  function Vo(e2, t2) {
    let o = e2.parentNode;
    if (o) {
      let r = o.nodeName.toUpperCase();
      r === "OPTGROUP" && (o = o.parentNode, r = o && o.nodeName.toUpperCase()), r === "SELECT" && !o.hasAttribute("multiple") && (e2.hasAttribute("selected") && !t2.selected && (e2.setAttribute("selected", "selected"), e2.removeAttribute("selected")), o.selectedIndex = -1);
    }
    ut(e2, t2, "selected");
  }
  function Xo(e2, t2) {
    ut(e2, t2, "checked"), ut(e2, t2, "disabled"), e2.value !== t2.value && (e2.value = t2.value), t2.hasAttribute("value") || e2.removeAttribute("value");
  }
  function Ft(e2, t2) {
    let { value: o } = t2;
    e2.value !== o && (e2.value = o);
    let { firstChild: r } = e2;
    if (r) {
      let { nodeValue: s } = r;
      if (s === o || !o && s === e2.placeholder)
        return;
      r.nodeValue = o;
    }
  }
  function jo(e2, t2) {
    if (!t2.hasAttribute("multiple")) {
      let o = 0, r = -1, s = e2.firstElementChild, i2, a2;
      for (; s; )
        if (a2 = s.nodeName && s.nodeName.toUpperCase(), a2 === "OPTGROUP")
          i2 = s, s = i2.firstElementChild;
        else {
          if (a2 === "OPTION") {
            if (s.hasAttribute("selected")) {
              r = o;
              break;
            }
            o++;
          }
          s = s.nextElementSibling, !s && i2 && (s = i2.nextElementSibling, i2 = null);
        }
      e2.selectedIndex = r;
    }
  }
  function Hr(e2, t2) {
    return !t2 || t2 === "http://www.w3.org/1999/xhtml" ? document.createElement(e2) : document.createElementNS(t2, e2);
  }
  function dt(e2, t2) {
    if (e2 === t2)
      return true;
    let o = e2.charCodeAt(0), r = t2.charCodeAt(0);
    return o <= 90 && r >= 97 ? e2 === t2.toUpperCase() : r <= 90 && o >= 97 ? t2 === e2.toUpperCase() : false;
  }
  function kr(e2, t2) {
    switch (e2.nodeName) {
      case "INPUT":
        Xo(e2, t2);
        break;
      case "OPTION":
        Vo(e2, t2);
        break;
      case "SELECT":
        jo(e2, t2);
        break;
      case "TEXTAREA":
        Ft(e2, t2);
        break;
    }
  }
  function se(e2) {
    return e2 && "getAttribute" in e2 ? e2.getAttribute("id") : void 0;
  }
  function Dr(e2, t2) {
    let o = e2.firstChild, r;
    for (; o; )
      r = o.nextSibling, t2.appendChild(o), o = r;
    return t2;
  }
  function gt(e2, t2, o, r = true) {
    Ve(e2), t2 && t2.removeChild(e2), Yo(e2, r, o);
  }
  function Wr(e2, t2, o) {
    let r = t2.firstChild, s, i2, a2 = e2.firstChild, c, f2, l2, u2;
    e:
      for (; r; ) {
        for (s = se(r), i2 = r.nextSibling; a2; ) {
          if (l2 = a2.nextSibling, r.isEqualNode(a2)) {
            r = i2, a2 = l2;
            continue e;
          }
          c = se(a2), f2 = a2.nodeType;
          let d;
          if (f2 === r.nodeType && (f2 === 1 ? (s ? s !== c && ((u2 = o.s.get(s)) ? l2.isEqualNode(u2) ? d = false : (e2.insertBefore(u2, a2), c ? o.i.add(c) : gt(a2, e2, o), a2 = u2, c = se(a2)) : d = false) : c && (d = false), d = d !== false && dt(a2.nodeName, r.nodeName), d && ht(a2, r, o)) : (f2 === 3 || f2 === 8) && (d = true, a2.nodeValue !== r.nodeValue && (a2.nodeValue = r.nodeValue))), d) {
            r = i2, a2 = l2;
            continue e;
          }
          c ? o.i.add(c) : gt(a2, e2, o), a2 = l2;
        }
        s && (u2 = o.s.get(s)) && dt(u2.nodeName, r.nodeName) ? (e2.appendChild(u2), ht(u2, r, o)) : (r.actualize && (r = r.actualize(e2.ownerDocument || document)), e2.appendChild(r), Vt(r, o)), r = i2, a2 = l2;
      }
    Fr(e2, a2, c, o), kr(e2, t2);
  }
  function ht(e2, t2, o) {
    let r = se(t2);
    if (r && o.s.delete(r), e2.isEqualNode(t2))
      return;
    let s = e2.getAttribute(n.qs.c);
    s !== "false" && (s !== "children" && Fo(e2, t2), e2.nodeName !== "TEXTAREA" ? Wr(e2, t2, o) : Ft(e2, t2));
  }
  function Yo(e2, t2, o) {
    if (e2.nodeType !== 1)
      return;
    let r = e2.firstChild;
    for (; r; ) {
      let s;
      t2 && (s = se(r)) ? o.i.add(s) : (Ve(r), r.firstChild && Yo(r, t2, o)), r = r.nextSibling;
    }
  }
  function Vt(e2, t2) {
    (e2.nodeType === 1 || e2.nodeType === 11) && ko(e2);
    let o = e2.firstChild;
    for (; o; ) {
      let r = o.nextSibling, s = se(o);
      if (s) {
        let i2 = t2.s.get(s);
        i2 && dt(o.nodeName, i2.nodeName) ? (o.parentNode.replaceChild(i2, o), ht(i2, o, t2)) : Vt(o, t2);
      } else
        Vt(o, t2);
      o = r;
    }
  }
  function Fr(e2, t2, o, r) {
    for (; t2; ) {
      let s = t2.nextSibling;
      (o = se(t2)) ? r.i.add(o) : gt(t2, e2, r), t2 = s;
    }
  }
  function Bo(e2, t2) {
    if (e2.nodeType === 1 || e2.nodeType === 11) {
      let o = e2.firstChild;
      for (; o; ) {
        let r = se(o);
        r && t2.s.set(r, o), Bo(o, t2), o = o.nextSibling;
      }
    }
  }
  function G(e2, t2) {
    let o = t2.cloneNode(true), r = m({ i: B(), s: K() });
    o.nodeType === 11 && (o = o.firstElementChild), Bo(e2, r);
    let s = e2, i2 = s.nodeType, a2 = o.nodeType;
    if (i2 === 1)
      a2 === 1 ? dt(e2.nodeName, o.nodeName) || (Ve(e2), s = Dr(e2, Hr(o.nodeName, o.namespaceURI))) : s = o;
    else if (i2 === 3 || i2 === 8) {
      if (a2 === i2)
        return s.nodeValue !== o.nodeValue && (s.nodeValue = o.nodeValue), s;
      s = o;
    }
    if (s.isEqualNode(o))
      Ve(e2);
    else {
      if (o.isEqualNode(s))
        return s;
      if (ht(s, o, r), r.i.size > 0) {
        for (let c of r.i)
          if (r.s.has(c)) {
            let f2 = r.s.get(c);
            gt(f2, f2.parentNode, r, false);
          }
      }
    }
    return s !== e2 && e2.parentNode && (s.actualize && (s = s.actualize(e2.ownerDocument || document)), e2.parentNode.replaceChild(s, e2)), r.s.clear(), r.i.clear(), s;
  }
  function Xe(e2, t2) {
    if (!(e2 instanceof Element))
      return false;
    let o = e2.closest(t2);
    return o && o.tagName === "A" ? o : false;
  }
  function Uo(e2) {
    if (e2.nodeName !== "A")
      return false;
    let t2 = e2.getAttribute("href");
    return !t2 || !Xt(t2) ? false : w(j(t2)) === false;
  }
  function Ko(e2, t2) {
    let o = [];
    return te(e2, (r) => {
      if (r.nodeName !== "A") {
        let s = r.querySelectorAll(t2);
        te(s, (i2) => {
          Uo(i2) && o.push(i2);
        });
      } else if (r.hasAttribute("href")) {
        let { href: s } = r;
        Xt(s) && w(j(s)) && o.push(r);
      }
    }), o;
  }
  var je = (e2) => {
    let t2 = [];
    return te(e2, (o) => {
      Uo(o) && t2.push(o);
    }), t2;
  };
  function _o(e2) {
    let t2 = Xe(e2.target, n.qs.l);
    if (!t2)
      return;
    let o = I(t2, 10);
    if (w(o.key) || o.key in E.r)
      return;
    t2.addEventListener(`${q}leave`, zo, { once: true });
    let r = C(o), s = r.threshold || n.config.hover.threshold;
    ot(o.key, function() {
      N("prefetch", t2, o) && O(r).then(function() {
        delete E.r[o.key], Go(t2);
      });
    }, s);
  }
  function zo(e2) {
    let t2 = Xe(e2.target, n.qs.l);
    t2 && tt(j(t2.href));
  }
  function Vr(e2) {
    e2.addEventListener(`${q}enter`, _o);
  }
  function Go(e2) {
    e2.removeEventListener(`${q}enter`, _o), e2.removeEventListener(`${q}leave`, zo);
  }
  function Ye() {
    !n.config.hover || n.observe.hover || (Y(Vr, je(n.qs.l)), n.observe.hover = true);
  }
  function he() {
    n.observe.hover && (Y(Go, je(n.qs.l)), n.observe.hover = false);
  }
  var ye;
  function Xr(e2) {
    return __async(this, null, function* () {
      if (e2.isIntersecting) {
        let t2 = I(e2.target, 11);
        if (!N("prefetch", e2.target, t2))
          return ye.unobserve(e2.target);
        (yield O(C(t2))) ? ye.unobserve(e2.target) : (p(3, `Prefetch will retry at next intersect for: ${t2.key}`), ye.observe(e2.target));
      }
    });
  }
  function Be() {
    if (!n.config.intersect || n.observe.intersect)
      return;
    ye || (ye = new IntersectionObserver(Y(Xr), n.config.intersect));
    let e2 = Y((o) => ye.observe(o)), t2 = Ko(n.qs.I, n.qs.O);
    e2(t2), n.observe.intersect = true;
  }
  function Ee() {
    n.observe.intersect && (ye.disconnect(), n.observe.intersect = false);
  }
  var Ue = {};
  yr(Ue, { connect: () => J, disconnect: () => $e, hook: () => ie, teardown: () => xe });
  function xe() {
    for (let e2 in n.components.reference)
      delete n.components.reference[e2];
    for (let e2 of n.components.instances.values())
      for (let t2 in e2.scope.events)
        Fe(e2, e2.scope.events[t2]);
    n.components.elements.clear(), n.components.instances.clear(), n.components.connected.clear(), p(2, "Component instances were disconnected");
  }
  function ie(e2, t2) {
    if (e2 === "onvisit")
      return H(() => ie(null, t2));
    e2 === null && (e2 = "onvisit");
    let { connected: o, instances: r } = n.components;
    e2 === "onload" && U("components", no(o));
    for (let s of o) {
      let i2 = r.get(s);
      i2 && e2 in i2 && i2[e2].apply(i2, t2);
    }
  }
  function J() {
    n.config.components && (n.observe.components || (n.page.type === 0 ? mt() : (R && (at(R), Ho()), ie("onload", [n.page])), n.observe.components = true));
  }
  function $e() {
    n.observe.components && (ie("onexit", [n.page]), n.observe.components = false);
  }
  var yt = new MutationObserver(function([e2]) {
    if (e2.type !== "childList")
      return;
    let t2 = e2.addedNodes.length;
    if (t2 || e2.removedNodes.length > 0) {
      let [o] = t2 ? e2.addedNodes : e2.removedNodes;
      if (o.nodeType !== 1)
        return;
      n.eval && po.test(o.nodeName) ? o.parentNode.nodeName === "HEAD" ? t2 ? wt("appendChild", o) : wt("removeChild", o) : jr(o) && !n.resources.has(o) ? n.resources.add(o) : n.resources.delete(o) : o instanceof HTMLElement && t2 && !o.hasAttribute(n.qs.e) && mt(o);
    }
  });
  function jr(e2) {
    let t2 = A().querySelectorAll(`${n.page.target.join(",")},[${n.qs.m}]`);
    for (let o = 0, r = t2.length; o < r; o++)
      if (t2[o].contains(e2))
        return false;
    return true;
  }
  function Ke() {
    n.observe.mutations || (yt.observe(document.head, { childList: true }), yt.observe(A(), { childList: true, subtree: true }), n.observe.mutations = true);
  }
  function _e() {
    if (n.observe.mutations) {
      yt.takeRecords(), yt.disconnect();
      for (let e2 of n.resources)
        A().removeChild(e2), n.resources.delete(e2);
      n.observe.mutations = false;
    }
  }
  function Yr({ clientX: e2, clientY: t2 }, o) {
    return e2 <= o.right && e2 >= o.left && t2 <= o.bottom && t2 >= o.top;
  }
  function Br(e2) {
    let t2 = e2.getBoundingClientRect(), o = e2.getAttribute(n.qs.x), r = Ge.test(o) ? Number(o) : n.config.proximity.distance;
    return { target: e2, top: t2.top - r, bottom: t2.bottom + r, left: t2.left - r, right: t2.right + r };
  }
  function Ur(e2) {
    let t2 = false;
    return (o) => {
      if (t2)
        return;
      t2 = true;
      let r = e2.findIndex((s) => Yr(o, s));
      if (r === -1)
        setTimeout(() => {
          t2 = false;
        }, n.config.proximity.throttle);
      else {
        let { target: s } = e2[r], i2 = C(I(s, 12)), a2 = i2.threshold || n.config.proximity.threshold;
        ot(i2.key, () => __async(this, null, function* () {
          if (!N("prefetch", s, i2))
            return Z();
          (yield O(i2)) && (e2.splice(r, 1), t2 = false, e2.length === 0 && (Z(), p(2, "Proximity observer disconnected")));
        }), a2);
      }
    };
  }
  var Bt;
  function ze() {
    if (!n.config.proximity || n.observe.proximity)
      return;
    let e2 = je(n.qs.x).map(Br);
    e2.length > 0 && (Bt = Ur(e2), addEventListener(`${q}move`, Bt, { passive: true }), n.observe.proximity = true);
  }
  function Z() {
    n.observe.proximity && (removeEventListener(`${q}move`, Bt), n.observe.proximity = false);
  }
  function Kr(e2, t2) {
    return __async(this, null, function* () {
      if (!n.eval || !e2 || !t2)
        return;
      let o = [], r = B(), { children: s } = e2;
      for (let c = 0, f2 = t2.length; c < f2; c++)
        r.add(t2[c].outerHTML);
      for (let c = 0, f2 = s.length; c < f2; c++) {
        let l2 = s[c], { nodeName: u2, outerHTML: d } = l2, y = true;
        u2 === "SCRIPT" ? y = l2.matches(n.qs.R) : u2 === "STYLE" ? y = l2.matches(n.qs.P) : u2 === "META" ? y = l2.matches(n.qs.C) : u2 === "LINK" ? y = l2.matches(n.qs.N) : y = e2.getAttribute(n.qs.b) !== "false", r.has(d) ? y ? o.push(l2) : r.delete(d) : o.push(l2);
      }
      let i2 = [], a2 = document.createRange();
      for (let c of r) {
        let f2 = a2.createContextualFragment(c).firstChild;
        if (T(f2, "href") || T(f2, "src")) {
          let u2 = new Promise(function(d) {
            f2.addEventListener("error", (y) => {
              p(3, `Resource <${f2.nodeName.toLowerCase()}> failed:`, f2), d();
            }), f2.addEventListener("load", () => d());
          });
          i2.push(u2);
        }
        e2.appendChild(f2), r.delete(c);
      }
      for (let c = 0, f2 = o.length; c < f2; c++)
        e2.removeChild(o[c]);
      yield Promise.all(i2);
    });
  }
  function _r(e2, t2) {
    let o = A();
    if (e2.selector === "body")
      G(o, t2.body);
    else if (e2.selector !== null) {
      let r = o.querySelectorAll(e2.selector), s = t2.body.querySelectorAll(e2.selector), i2 = B();
      for (let a2 = 0, c = r.length; a2 < c; a2++) {
        let f2 = r[a2], l2 = s[a2];
        !l2 || !f2 || N("render", f2, l2) && (f2.isEqualNode(l2) || i2.add(G(f2, l2)));
      }
      e2.type !== 6 && R && R.n.length > 0 && H(() => {
        qt(t2.body, o, R.n, i2), U("type", 6), i2.clear();
      });
    } else
      for (let r of e2.fragments) {
        let s = n.fragments.get(r), i2 = t2.getElementById(r);
        !i2 || !s || N("render", s, i2) && (s.isEqualNode(i2) || (G(s, i2), R && R.n.length > 0 && H(() => st(i2, R.n))));
      }
  }
  function zr(e2, t2) {
    let o = e2.hydrate;
    if (o.length === 1 && o[0] === "body") {
      G(A(), t2.body);
      return;
    }
    let r = o.join(","), s = A().querySelectorAll(r), i2 = e2.preserve && e2.preserve.length > 0 ? e2.preserve.join(",") : null, a2 = [];
    if (i2) {
      let c = A().querySelectorAll(i2);
      for (let f2 = 0, l2 = c.length; f2 < l2; f2++) {
        let u2 = c[f2];
        u2.setAttribute(n.qs.c, "false"), a2.push(u2);
      }
    }
    if (s.length > 0) {
      let c = t2.body.querySelectorAll(r);
      for (let f2 = 0, l2 = s.length; f2 < l2; f2++) {
        let u2 = s[f2], d = c[f2];
        if (c[f2] instanceof HTMLElement) {
          if (!N("hydrate", u2, d))
            continue;
          G(d, d);
        }
      }
    }
    if (i2)
      for (let c of a2)
        c.removeAttribute("spx-morph");
    e2.hydrate = void 0, e2.preserve = void 0, e2.type = 6, k(e2);
  }
  function k(e2) {
    he(), Ee(), Z(), _e(), $e(), ct(), n.eval || (document.title = e2.title);
    let t2 = z(e2.snap);
    return e2.type === 7 ? zr(e2, t2) : (Kr(io(), t2.head.children), _r(e2, t2), scrollTo(e2.scrollX, e2.scrollY)), me.done(), H(() => {
      Ye(), Be(), ze(), J(), Ke();
    }), N("load", e2), e2;
  }
  var g = window.history;
  function Zo() {
    return g.state !== null && "rev" in g.state && g.state.key !== g.state.rev;
  }
  function _t(e2) {
    if (g.state == null || typeof g.state != "object")
      return false;
    let t2 = Re(g.state)(["key", "rev", "scrollX", "scrollY", "title"]);
    return typeof e2 == "string" ? t2 && g.state.key === e2 : t2;
  }
  function Qo() {
    return __async(this, null, function* () {
      yield xo(), n.loaded = true;
    });
  }
  function Gr(e2) {
    return _t(e2.key) ? (scrollTo(g.state.scrollX, g.state.scrollY), $(e2, g.state)) : ve(e2), e2;
  }
  function ve({ key: e2, rev: t2, title: o, scrollX: r, scrollY: s }) {
    let i2 = { key: e2, rev: t2, scrollX: r, scrollY: s, title: o || document.title };
    return g.replaceState(i2, i2.title, i2.key), p(1, `History replaceState: ${g.state.key}`), g.state;
  }
  function Te({ key: e2, rev: t2, title: o }) {
    let r = { key: e2, rev: t2, title: o, scrollY: 0, scrollX: 0 };
    return g.pushState(r, r.title, r.key), p(1, `History pushState: ${g.state.key}`), g.state;
  }
  function er(e2) {
    return __async(this, null, function* () {
      if ((n.loaded || n.ready) && e2.state !== null)
        if (w(e2.state.key)) {
          !w(e2.state.rev) && e2.state.rev !== e2.state.key && rt(e2.state.rev);
          let t2 = n.pages[e2.state.key];
          t2.type === 4 ? (p(1, `History popState reverse (snapshot): ${t2.key}`), xe()) : p(1, `History popState session (snapshot): ${t2.key}`), U("type", 5), k(t2);
        } else {
          p(1, `History popState fetch: ${e2.state.key}`), xe(), e2.state.type = 5;
          let t2 = yield O(e2.state);
          if (!t2)
            return location.assign(e2.state.key);
          let o = j(location);
          if (t2.key === o)
            k(t2);
          else if (w(o))
            k(n.pages[o]);
          else {
            xe();
            let r = C(I(o, 5));
            yield O(r), Te(r);
          }
        }
    });
  }
  function tr(e2) {
    if (!n.observe.history)
      return g.scrollRestoration && (g.scrollRestoration = "manual"), addEventListener("popstate", er, false), addEventListener("load", Qo, false), n.observe.history = true, typeof e2 == "object" && e2.type === 0 ? Gr(e2) : e2;
  }
  function or() {
    n.observe.history && (g.scrollRestoration && (g.scrollRestoration = "auto"), removeEventListener("popstate", er, false), removeEventListener("load", Qo, false), n.observe.history = false);
  }
  function C(e2) {
    let t2 = Re(e2);
    return e2.ts = Ze(), e2.target = Pt(e2), t2("selector") || (e2.target[0] === "body" ? e2.selector = "body" : e2.selector = we(e2.target)), n.config.cache && (t2("cache") || (e2.cache = n.config.cache), t2("snap") || (e2.snap = W())), t2("scrollY") || (e2.scrollY = 0), t2("scrollX") || (e2.scrollX = 0), n.config.hover !== false && e2.type === 10 && (t2("threshold") || (e2.threshold = n.config.hover.threshold)), n.config.proximity !== false && e2.type === 12 && (t2("proximity") || (e2.proximity = n.config.proximity.distance), t2("threshold") || (e2.threshold = n.config.proximity.threshold)), n.config.progress && !t2("progress") && (e2.progress = n.config.progress.threshold), t2("fragments") || (e2.fragments = n.config.fragments), t2("history") || (e2.history = true), t2("visits") || (e2.visits = 0), t2("components") || (e2.components = []), n.pages[e2.key] = e2, n.pages[e2.key];
  }
  function rr(e2) {
    let t2 = $(m(e2), { target: [], selector: null, cache: n.config.cache, history: true, scrollX: 0, scrollY: 0, fragments: n.config.fragments });
    return n.config.hover && (t2.threshold = n.config.hover.threshold), n.config.proximity && (t2.proximity = n.config.proximity.distance, t2.threshold = n.config.proximity.threshold), n.config.progress && (t2.progress = n.config.progress.threshold), t2;
  }
  function U(e2, t2, o = g.state.key) {
    return e2 === "location" ? n.pages[o][e2] = $(n.pages[o][e2], t2) : e2 === "target" ? (n.pages[o].target = Pt(t2), n.pages[o].selector = we(n.pages[o].target)) : e2 === "components" ? n.pages[o].components = t2 : n.pages[o][e2] = t2, n.pages[o];
  }
  function Ne(e2, t2) {
    let o = N("before:cache", e2, t2), r = typeof o == "string" ? o : t2;
    return e2.type > 5 && e2.type > 9 && (e2.type = 1), e2.title = $t(t2), !n.config.cache || o === false ? e2 : "snap" in e2 ? (n.pages[e2.key] = e2, n.snaps[e2.snap] = r, wo(e2), N("after:cache", e2), ie("oncache", e2), e2) : ae(e2, r);
  }
  function ae(e2, t2) {
    let o = e2.key in n.pages ? n.pages[e2.key] : C(e2);
    return typeof t2 == "string" && (n.snaps[o.snap] = t2, e2.title = $t(t2)), $(o, e2);
  }
  function it(e2, t2) {
    let o = t2 = t2 ? t2.charCodeAt(0) === 47 ? t2 in n.pages ? n.pages[t2].snap : null : t2 : n.page.snap;
    o ? n.snaps[o] = e2 : p(3, "Snapshot record does not exist, update failed");
  }
  function So(e2) {
    if (!e2) {
      if (g.state === null) {
        p(3, "Missing history state reference, page cannot be returned");
        return;
      }
      e2 = g.state.key;
    }
    if (e2 in n.pages)
      return X(m(), { page: { get: () => n.pages[e2] }, dom: { get: () => ue(n.snaps[n.pages[e2].snap]) } });
    p(5, `No record exists: ${e2}`);
  }
  function z(e2) {
    let t2 = e2 = e2 ? e2.charCodeAt(0) === 47 ? n.pages[e2].snap : e2 : n.page.snap;
    return ue(n.snaps[t2]);
  }
  function qo({ mounted: e2 = null } = {}) {
    let t2 = m();
    for (let o of n.components.instances.values()) {
      let { scope: r } = o;
      n.components.connected.has(r.key) && (e2 !== null && r.mounted === e2 || (r.alias !== null && !(r.alias in t2) ? t2[r.alias] = [o] : r.instanceOf in t2 ? t2[r.instanceOf].push(o) : t2[r.instanceOf] = [o]));
    }
    return Pe(t2) ? null : t2;
  }
  function nr(e2) {
    if (!e2) {
      if (g.state === null) {
        p(3, "Missing history state reference, page cannot be returned");
        return;
      }
      e2 = g.state.key;
    }
    if (e2 in n.pages)
      return n.pages[e2];
    p(5, `No page record exists for: ${e2}`);
  }
  function w(e2) {
    return T(n.pages, e2) && T(n.pages[e2], "snap") && T(n.snaps, n.pages[e2].snap) && typeof n.snaps[n.pages[e2].snap] == "string";
  }
  function Q(e2) {
    e2 ? typeof e2 == "string" ? (delete n.snaps[n.pages[e2].snap], delete n.pages[e2]) : M(e2) && Y((t2) => {
      delete n.snaps[n.pages[t2].snap], delete n.pages[t2];
    }, e2) : (Ct(n.pages), Ct(n.snaps));
  }
  var L = le.replace(/(?:https?:)?(?:\/\/(?:www\.)?|(?:www\.))/, x);
  function Jt(e2, t2) {
    let o = t2 ? rr(t2) : m(), r = e2.getAttributeNames();
    for (let s = 0, i2 = r.length; s < i2; s++) {
      let a2 = r[s];
      if (a2.startsWith(n.qs.d)) {
        T(o, "data") || (o.data = m());
        let c = pe(a2.slice(n.qs.d.length)), f2 = e2.getAttribute(a2).trim();
        qe.test(f2) ? o.data[c] = f2 === "NaN" ? NaN : +f2 : Se.test(f2) ? o.data[c] = f2 === "true" : f2.charCodeAt(0) === 123 || f2.charCodeAt(0) === 91 ? o.data[c] = Ie(a2, f2) : o.data[c] = f2;
      } else {
        if (!n.qs.v.test(a2))
          continue;
        let c = e2.getAttribute(a2).trim();
        if (a2 === "href")
          o.rev = location.pathname + location.search, t2 || (o.location = Gt(c), o.key = o.location.pathname + o.location.search);
        else {
          let f2 = a2.slice(a2.lastIndexOf("-") + 1), l2 = c.replace(fo, x).trim();
          if (f2 === "target")
            o[f2] = l2 === "true" ? [] : l2 !== x ? go(l2) : [], o.selector = we(o[f2]);
          else if (mo.test(l2)) {
            let u2 = l2.match(/\[?[^,'"[\]()\s]+\]?/g);
            o[f2] = co.test(f2) ? u2.reduce(bo(2), []) : u2;
          } else if (f2 === "position")
            if (Rt.test(l2)) {
              let u2 = l2.match(Rt);
              o[`scroll${u2[0].toUpperCase()}`] = +u2[1], u2.length === 4 && (o[`scroll${u2[2].toUpperCase()}`] = +u2[3]);
            } else
              p(3, `Invalid attribute value on <${a2}>, expected: y:number or x:number`, e2);
          else
            f2 === "scroll" ? Ge.test(l2) ? o.scrollY = +l2 : p(3, `Invalid attribute value on <${a2}>, expected: number`, e2) : Se.test(l2) && !lo.test(a2) ? o[f2] = l2 === "true" : qe.test(l2) ? o[f2] = +l2 : (f2 === "history" && l2 !== "push" && l2 !== "replace" && p(5, `Invalid attribute value on <${a2}>, expected: false, push or replace`, e2), o[f2] = l2);
        }
      }
    }
    return o;
  }
  function Oe(e2) {
    let t2 = m();
    if (e2.length === 1 && e2.charCodeAt(0) === 47)
      return t2.pathname = e2, t2.hash = x, t2.search = x, t2;
    let r = e2.indexOf("#");
    r >= 0 ? (t2.hash = e2.slice(r), e2 = e2.slice(0, r)) : t2.hash = x;
    let s = e2.indexOf("?");
    return s >= 0 ? (t2.search = e2.slice(s), e2 = e2.slice(0, s)) : t2.search = x, t2.pathname = e2, t2;
  }
  function xt(e2, t2) {
    let o = e2.indexOf("/", t2);
    if (o > t2) {
      let s = e2.indexOf("#", o);
      return s < 0 ? e2.slice(o) : e2.slice(o, s);
    }
    let r = e2.indexOf("?", t2);
    if (r > t2) {
      let s = e2.indexOf("#", r);
      return s < 0 ? e2.slice(r) : e2.slice(r, s);
    }
    return e2.length - t2 === L.length ? "/" : null;
  }
  function zt(e2) {
    let t2 = e2.startsWith("www.") ? e2.slice(4) : e2, o = t2.indexOf("/");
    if (o >= 0) {
      let r = t2.slice(o);
      if (t2.slice(0, o) === L)
        return r.length > 0 ? Oe(r) : Oe("/");
    } else {
      let r = t2.search(/[?#]/);
      if (r >= 0) {
        if (t2.slice(0, r) === L)
          return Oe("/" + t2.slice(r));
      } else if (t2 === L)
        return Oe("/");
    }
    return null;
  }
  function Jr(e2) {
    return e2.startsWith("http") ? 1 : e2.startsWith("//") ? 2 : e2.startsWith("www.") ? 3 : 0;
  }
  function Xt(e2) {
    if (typeof e2 != "string" || e2.length === 0)
      return false;
    if (e2.charCodeAt(0) === 47)
      return e2.charCodeAt(1) !== 47 ? true : e2.startsWith("www.", 2) ? e2.startsWith(L, 6) : e2.startsWith(L, 2);
    if (e2.charCodeAt(0) === 63)
      return true;
    if (e2.startsWith("www."))
      return e2.startsWith(L, 4);
    if (e2.startsWith("http")) {
      let t2 = e2.indexOf("/", 4) + 2;
      return e2.startsWith("www.", t2) ? e2.startsWith(L, t2 + 4) : e2.startsWith(L, t2);
    }
  }
  function Zr(e2) {
    return e2.charCodeAt(0) === 47 ? e2.charCodeAt(1) !== 47 ? Oe(e2) : zt(e2.slice(2)) : e2.charCodeAt(0) === 63 ? Oe(location.pathname + e2) : e2.startsWith("https:") || e2.startsWith("http:") ? zt(e2.slice(e2.indexOf("/", 4) + 2)) : e2.startsWith("www.") ? zt(e2) : null;
  }
  function j(e2) {
    if (typeof e2 == "object")
      return e2.pathname + e2.search;
    if (e2 === x)
      return "/";
    let t2 = Jr(e2);
    if (t2 === 1) {
      let o = e2.charCodeAt(4) === 115 ? 8 : 7, r = e2.startsWith("www.", o) ? o + 4 : o;
      return e2.startsWith(L, r) ? xt(e2, r) : null;
    }
    if (t2 === 2) {
      let o = e2.startsWith("www.", 2) ? 6 : 2;
      return e2.startsWith(L, o) ? xt(e2, o) : null;
    }
    return t2 === 3 ? e2.startsWith(L, 4) ? xt(e2, 4) : null : e2.startsWith(L, 0) ? xt(e2, 0) : null;
  }
  function sr() {
    return m({ hostname: L, origin: le, pathname: location.pathname, search: location.search, hash: location.hash });
  }
  function Gt(e2) {
    if (e2 === x)
      return sr();
    let t2 = Zr(e2);
    return t2 === null && p(5, `Invalid pathname: ${e2}`), t2.origin = le, t2.hostname = L, t2;
  }
  function I(e2, t2 = 6) {
    if (e2 instanceof Element) {
      let r = Jt(e2);
      return r.type = t2 || 6, r;
    }
    let o = m();
    return e2 === 0 ? (o.location = sr(), o.key = o.rev = j(o.location), o.type = e2, o.visits = 1, n.index = o.key) : t2 === 7 ? (o.location = Gt(e2), o.key = o.rev = j(o.location), o.type = t2) : (o.rev = location.pathname + location.search, o.location = Gt(typeof e2 == "string" ? e2 : o.rev), o.key = j(o.location), o.type = t2), o;
  }
  function Qr(e2) {
    return !(e2.target && e2.target.isContentEditable || e2.defaultPrevented || e2.button > 1 || e2.altKey || e2.ctrlKey || e2.metaKey || e2.shiftKey);
  }
  var V = function(e2) {
    if (!Qr(e2))
      return;
    let t2 = Xe(e2.target, n.qs.A);
    if (!t2)
      return;
    let o = j(t2.href);
    if (o === null)
      return;
    let r = o === n.page.key;
    console.log(o, n.page.key, n.prev.key);
    let s = () => {
      p(3, `Drag occurance deteced, cancelled visit: ${o}`), V.drag = true, t2.removeEventListener(`${q}move`, s);
    };
    if (t2.addEventListener(`${q}move`, s, { once: true }), V.drag === true)
      return V.drag = false, V(e2);
    if (t2.removeEventListener(`${q}move`, s), !N("visit", e2))
      return;
    let i2 = (a2, c = true) => {
      n.pages[a2.key].ts = Ze(), n.pages[a2.key].visits = a2.visits + 1, n.pages[a2.rev].scrollX = window.scrollX, n.pages[a2.rev].scrollY = window.scrollY, ie("onvisit", a2), r ? (p(1, `URL Pathname matches current route: ${o}`), k(a2)) : (ve(n.pages[a2.rev]), c ? (Te(a2), k(a2)) : ir(a2));
    };
    if (he(), Z(), Ee(), w(o)) {
      let a2 = Jt(t2, n.pages[o]), c = ae(a2);
      t2.onclick = (f2) => {
        f2.preventDefault(), i2(c);
      };
    } else if (E.a.has(o)) {
      St(o), p(2, `Request in transit: ${o}`);
      let a2 = n.pages[o];
      t2.onclick = (c) => {
        c.preventDefault(), i2(a2, false);
      };
    } else {
      St(), tt(o);
      let a2 = C(I(t2, 6));
      O(a2), t2.onclick = (c) => {
        c.preventDefault(), i2(a2, false);
      };
    }
  };
  function ir(e2) {
    return __async(this, null, function* () {
      e2.progress && me.start(e2.progress);
      let t2 = yield De(e2);
      t2 ? (Te(t2), k(t2)) : location.assign(e2.key);
    });
  }
  function Zt(e2, t2) {
    return __async(this, null, function* () {
      if (t2) {
        typeof t2.cache == "string" && (t2.cache === "clear" ? Q() : Q(t2.key)), t2.progress && me.start(t2.progress);
        let o = yield O(t2);
        o ? (Te(o), k(o)) : location.assign(t2.key);
      } else
        return ir(n.pages[e2]);
    });
  }
  function ar() {
    n.observe.hrefs || (V.drag = false, Me === "mouseOnly" ? addEventListener(`${q}down`, V, false) : (Me === "touchOnly" || addEventListener(`${q}down`, V, false), addEventListener("touchstart", V, false)), n.observe.hrefs = true);
  }
  function cr() {
    n.observe.hrefs && (Me === "mouseOnly" ? removeEventListener(`${q}down`, V, false) : (Me === "touchOnly" || removeEventListener(`${q}down`, V, false), removeEventListener("touchstart", V, false)), n.observe.hrefs = false);
  }
  function lr() {
    let e2 = I(0), t2 = tr(C(e2));
    X(n, { prev: { get() {
      return n.pages[g.state.rev];
    } }, page: { get() {
      return n.pages[g.state.key];
    } }, snapDom: { get() {
      return ue(n.snaps[n.page.snap]);
    } } });
    let o = () => {
      let r = Ne(t2, de());
      return ar(), ct(), n.config.manual === false && (Ye(), Be(), ze(), J(), Ke()), H(() => {
        U("type", 6), rt(r), No(r);
      }), N("x"), r;
    };
    return new Promise((r) => {
      let { readyState: s } = document;
      if (s === "interactive" || s === "complete")
        return r(o());
      addEventListener("DOMContentLoaded", () => r(o()));
    });
  }
  function pr() {
    he(), Ye(), Ee(), Be(), Z(), ze(), $e(), J(), _e(), Ke();
  }
  function mr() {
    or(), cr(), _e(), he(), Ee(), Z(), n.config.components && ($e(), xe(), n.components.registry.clear()), Q(), n.config.globalThis && delete window.spx, p(2, "Disconnected");
  }
  var ur = class {
    constructor(e2, t2) {
      this.state = m();
      X(this, { scope: { get() {
        return e2;
      } }, static: { get() {
        return t2;
      } } });
      let o = `${n.config.schema}${this.scope.instanceOf}`;
      if (this.state = new Proxy(m(), { set: (r, s, i2) => {
        let a2 = this.static.state[s], c = typeof i2 == "object" || M(i2) ? JSON.stringify(i2) : `${i2}`;
        if (typeof a2 == "object" && T(a2, "persist") && a2.persist ? r[s] = this.scope.state[s] = i2 : r[s] = i2, this.dom && c !== this.dom.getAttribute(`${o}:${s}`) && this.dom.setAttribute(`${o}:${s}`, c), s in this.scope.binds) {
          let { binds: f2 } = this.scope;
          for (let l2 in f2[s])
            f2[s][l2].value = c, n.components.elements.has(f2[s][l2].dom) && (n.components.elements.get(f2[s][l2].dom).innerText = c);
        }
        return true;
      } }), Pe(this.scope.state))
        for (let r in this.static.state) {
          let s = this.static.state[r], i2, a2;
          typeof s == "object" ? (i2 = s.typeof, a2 = s.default) : i2 = s, i2 === String ? this.state[r] = a2 || x : i2 === Boolean ? this.state[r] = a2 || false : i2 === Number ? this.state[r] = a2 ? +a2 : 0 : i2 === Array ? this.state[r] = a2 || [] : i2 === Object && (this.state[r] = a2 || {}), this.scope.state[r] = this.state[r];
        }
      else
        for (let r in t2.state) {
          if (!(r in this.scope.state))
            if (typeof t2.state[r] == "object")
              this.scope.state[r] = t2.state[r].default;
            else
              switch (t2.state[r]) {
                case String:
                  this.scope.state[r] = x;
                  break;
                case Boolean:
                  this.scope.state[r] = false;
                  break;
                case Number:
                  this.scope.state[r] = 0;
                  break;
                case Object:
                  this.scope.state[r] = {};
                  break;
                case Array:
                  this.scope.state[r] = [];
                  break;
              }
          let s = this.static.state[r], i2, a2 = this.dom.getAttribute(`${o}:${r}`), c, f2 = a2 !== null && a2 !== x;
          typeof s == "object" ? (i2 = s.typeof, c = f2, f2 || (a2 = s.default)) : i2 = s, `has${Le(r)}` in this.state || ee(this.state, `has${Le(r)}`, { get() {
            return f2;
          } }), typeof a2 == "string" && a2.startsWith("window.") ? this.state[r] = window[a2.slice(7)] : i2 === String ? this.state[r] = a2 || x : i2 === Boolean ? this.state[r] = a2 || false : i2 === Number ? this.state[r] = a2 ? +a2 : 0 : i2 === Array ? this.state[r] = f2 ? Ie(a2) : c ? a2 : [] : i2 === Object && (this.state[r] = f2 ? Ie(a2) : c ? a2 : {}), this.scope.state[r] = this.state[r];
        }
    }
    get dom() {
      return n.components.elements.get(this.scope.dom);
    }
    get html() {
      return this.dom.closest("html");
    }
  };
  var Qt = m({ $: n, Component: ur, on: Ot, off: Mt, observe: pr, connect: en, capture: an, form: fn, render: sn, session: on, reload: rn, fetch: nn, clear: Q, hydrate: ln, prefetch: cn, visit: pn, disconnect: mr, register: tn, get config() {
    return n.config;
  }, supported: !!(bt && window.history.pushState && window.requestAnimationFrame && window.DOMParser && window.Proxy), history: m({ get state() {
    return g.state;
  }, api: g, push: Te, replace: ve, has: _t, reverse: Zo }) });
  function en(e2 = {}) {
    if (bt === false)
      return p(5, "Invalid runtime environment: window is undefined.");
    if (!Qt.supported)
      return p(5, "Browser does not support SPX");
    if (!window.location.protocol.startsWith("http"))
      return p(5, "Invalid protocol, SPX expects HTTPS or HTTP protocol");
    Po(e2), n.config.globalThis && T(window, "spx") === false && Qe(window, "spx", Qt);
    let t2 = lr();
    return function(o) {
      return __async(this, null, function* () {
        let r = yield t2;
        if (o.constructor.name === "AsyncFunction")
          try {
            yield o(r);
          } catch (s) {
            p(3, "Connection Error", s);
          }
        else
          o(r);
        p(2, "Connection Established");
      });
    };
  }
  function tn(...e2) {
    if (typeof e2[0] == "string")
      e2.length > 2 && p(5, [`Named component registration expects 2 parameters, recieved ${e2.length}.`, 'Registry should follow this structure: spx.register("identifer", YourComponent)'], e2), oe({ [Ue[0]]: e2[1] });
    else
      for (let t2 of e2)
        if (M(t2))
          for (let o of t2)
            typeof o[0] == "string" ? oe({ [o[0]]: o[1] }) : typeof o == "function" && oe({ [et(o)]: o }, true);
        else {
          let o = typeof t2;
          o === "function" ? oe({ [et(t2)]: t2 }, true) : o === "object" && oe(t2);
        }
    n.ready ? J() : Ot("x", function t2() {
      J(), Mt("x", t2);
    });
  }
  function on() {
    return X(m(), { config: { get() {
      return n.config;
    } }, snaps: { get() {
      return n.snaps;
    } }, pages: { get() {
      return n.pages;
    } }, observers: { get() {
      return n.observe;
    } }, components: { get() {
      return n.components;
    } }, fragments: { get() {
      return n.fragments;
    } }, memory: { get() {
      let e2 = n.memory;
      return e2.size = Io(e2.bytes), e2;
    } } });
  }
  function rn() {
    return __async(this, null, function* () {
      let e2 = n.pages[g.state.key];
      e2.type = 9;
      let t2 = yield O(e2);
      return t2 ? (p(2, "Triggered reload, page was re-cached"), k(t2)) : (p(3, "Reload failed, triggering refresh (cache will purge)"), location.assign(e2.key));
    });
  }
  function nn(e2) {
    return __async(this, null, function* () {
      let t2 = I(e2, 2);
      t2.location.origin !== le && p(5, "Cross origin fetches are not allowed");
      let o = yield ke(t2.key);
      if (o)
        return o;
    });
  }
  function sn(e2, t2, o) {
    return __async(this, null, function* () {
      let r = n.page, s = I(e2);
      s.location.origin !== le && p(5, "Cross origin fetches are not allowed");
      let i2 = yield ke(s.key, { type: "document" });
      if (i2 || p(5, `Fetch failed for: ${s.key}`, i2), yield o.call(r, i2), t2 === "replace") {
        r.title = i2.title;
        let a2 = ae($(r, s), de(i2));
        return ve(a2), a2;
      } else
        return k(Ne(s, de(i2)));
    });
  }
  function an(e2) {
    let t2 = nr();
    if (!t2)
      return;
    let o = z();
    if (e2 = M(e2) ? e2 : t2.target, e2.length === 1 && e2[0] === "body") {
      G(o.body, A()), ae(t2, de(o));
      return;
    }
    let r = e2.join(","), s = A().querySelectorAll(r);
    te(o.body.querySelectorAll(r), (i2, a2) => {
      G(i2, s[a2]);
    }), ae(t2, de(o));
  }
  function cn(e2) {
    return __async(this, null, function* () {
      let t2 = I(e2, 1);
      if (w(t2.key)) {
        p(3, `Cache already exists for ${t2.key}, prefetch skipped`);
        return;
      }
      let o = yield O(C(t2));
      if (o)
        return o;
      p(5, `Prefetch failed for ${t2.key}`);
    });
  }
  function fn(e2, t2) {
    return __async(this, null, function* () {
      let o = new FormData();
      for (let s in t2.data)
        o.append(s, t2.data[s]);
      return yield ke(e2, { method: t2.method, body: o });
    });
  }
  function ln(e2, t2) {
    return __async(this, null, function* () {
      let o = I(e2, 7);
      if (O(o), M(t2)) {
        o.hydrate = [], o.preserve = [];
        for (let s of t2)
          s.charCodeAt(0) === 33 ? o.preserve.push(s.slice(1)) : o.hydrate.push(s);
      } else
        o.hydrate = n.config.fragments;
      let r = yield De(o);
      if (r) {
        let { key: s } = g.state;
        if (ve(r), k(r), o.key !== s) {
          n.index === s && (n.index = o.key);
          for (let i2 in n.pages)
            n.pages[i2].rev === s && (n.pages[i2].rev = o.key);
          Q(s);
        }
      }
      return z(r.key);
    });
  }
  function pn(e2, t2) {
    return __async(this, null, function* () {
      let o = I(e2), r = typeof t2 == "object" ? $(o, t2) : o;
      return w(o.key) ? Zt(o.key, ae(r)) : Zt(o.key, C(r));
    });
  }
  var Ic = Qt;

  // ../dist/index.js
  var { isArray: T2, from: C2 } = Array;
  var m2 = /* @__PURE__ */ new Map();
  var g2 = (t2, e2 = /* @__PURE__ */ Object.create(null)) => t2 ? Object.assign(e2, t2) : e2;
  var $2 = class extends Array {
    constructor() {
      super(...arguments);
      this.ref = g2();
      this.get = (s) => {
        let i2 = typeof s;
        if (i2 === "number")
          return this[s];
        if (i2 === "string") {
          if (s in this.ref)
            return this[this.ref[s]];
          console.error(`Relapse: Unknown fold id: ${s}`);
        }
        return this;
      };
    }
  };
  var w2 = (t2, e2 = 1) => {
    t2.ariaDisabled = e2 === 1 ? "true" : "false";
  };
  var h = (t2, e2, s) => t2.style.setProperty(e2, s);
  var L2 = (t2, e2) => t2.classList.add(e2);
  var R2 = (t2, e2) => t2.classList.remove(e2);
  var A2 = (t2, e2 = 1) => {
    t2.ariaExpanded = e2 === 1 ? "true" : "false";
  };
  function M2(t2, e2) {
    let s = t2, i2 = s.getAttribute(e2.schema);
    if ((i2 === null || i2 === "") && (i2 = s.hasAttribute("id") ? s.id : `R${m2.size}`, s.setAttribute(e2.schema, i2)), m2.has(i2)) {
      if (m2.get(i2).options.unique === false)
        return;
      throw new Error(`Relapse: Instance already exists with id: ${i2} `);
    }
    let l2 = D2(e2, s.attributes), f2 = new $2(), r = m2.set(i2, { id: i2, semantic: s.firstElementChild.nodeName === "DETAILS", openCount: 0, status: 1, active: NaN, events: g2(), get folds() {
      return f2;
    }, get options() {
      return l2;
    }, get element() {
      return s;
    }, set element(n2) {
      s = n2, i2 = s.getAttribute(e2.schema);
    } }).get(i2);
    s.ariaMultiSelectable = `${r.options.multiple}`;
    let u2 = O2(r.events), b = s.children.length, v = r.semantic ? 1 : 2, p2 = r.options.classes, y = 0;
    do {
      let n2 = g2({ index: r.folds.length, locked: false }), o = false, a2, d, c;
      if (r.semantic) {
        if (c = s.children[y], c.nodeName !== "DETAILS")
          throw new Error(`Relapse: Invalid markup on "${r.id}", expected: <details>`);
        a2 = c.firstElementChild, d = a2.nextElementSibling, o = c.hasAttribute("open");
      } else
        a2 = s.children[y], d = c = s.children[y + 1], d.hasAttribute("hidden") ? d.removeAttribute("hidden") : d.firstElementChild.hasAttribute("hidden") && d.firstElementChild.removeAttribute("hidden");
      c.setAttribute("role", "region");
      let x2 = a2.classList.contains(p2.disabled), H2 = a2.classList.contains(p2.opened), F = d.classList.contains(p2.expanded);
      a2.ariaExpanded === "true" || o || H2 ? (a2.ariaExpanded !== "true" && A2(a2), !H2 && !x2 && L2(a2, p2.opened), F || L2(d, p2.expanded), (x2 || a2.ariaDisabled === "true") && (L2(a2, p2.disabled), w2(a2), n2.disabled = true, n2.locked = true), n2.expanded = true) : a2.ariaDisabled === "true" || x2 ? (a2.ariaDisabled !== "true" && w2(a2), x2 || L2(a2, p2.disabled), H2 && R2(a2, p2.opened), n2.expanded = (F || o) === true, n2.disabled = true, n2.locked = true, A2(a2, n2.expanded ? 1 : 2)) : (n2.expanded = false, n2.disabled = false, A2(a2, 2), w2(a2, 2)), n2.id = `${r.id}-fold-${n2.index}`, a2.hasAttribute("id") || (a2.id = `${r.id}-button-${n2.index}`), d.hasAttribute("id") || (d.id = `${r.id}-content-${n2.index}`), a2.tabIndex = n2.index, a2.setAttribute("aria-controls", d.id), d.setAttribute("aria-labelledby", a2.id), n2.expanded ? (r.openCount = r.openCount + 1, r.options.multiple === false && r.openCount === 2 && console.warn(`Relapse: More than 1 fold is expanded but "multiple" is set to false on: ${r.id}`), r.semantic ? (n2.height = a2.offsetHeight + d.offsetHeight, h(c, "height", "auto")) : (n2.height = d.scrollHeight, h(c, "max-height", "inherit"))) : r.semantic ? (n2.height = a2.offsetHeight, h(c, "height", "auto")) : (n2.height = 0, h(d, "max-height", "0"), h(d, "overflow", "hidden")), n2.button = a2, n2.wrapper = c, n2.element = d, S(n2, r, u2), y = y + v;
    } while (y < b);
    r.on = u2.on, r.off = u2.off, r.collapse = (n2 = r.active) => {
      isNaN(n2) || f2.get(n2).close(n2);
    }, r.expand = (n2 = r.active) => {
      isNaN(n2) || f2.get(n2).open(n2);
    }, r.destroy = () => {
      f2.forEach((n2) => n2.destroy()), u2.emit("destroy", r), m2.delete(i2);
    }, r.reinit = () => {
      f2.forEach((n2) => n2.destroy()), M2(t2, e2);
    };
  }
  function O2(t2) {
    let e2 = g2();
    return e2.on = (s, i2, l2) => (t2[s] || (t2[s] = []), t2[s].push(l2 ? i2.bind(l2) : i2) - 1), e2.off = (s, i2) => {
      let l2 = t2[s], f2 = `Relapse: Removed ${s} event listener`;
      if (l2 && typeof i2 == "number")
        l2.splice(i2, 1), console.warn(`${f2} (id: ${i2})`), l2.length === 0 && delete l2[s];
      else {
        let r = [];
        if (l2 && typeof i2 == "function")
          for (let u2 = 0, b = l2.length; u2 < b; u2++)
            l2[u2] !== i2 ? r.push(l2[u2]) : console.warn(`${f2} (id: ${u2})`);
        r.length > 0 ? l2[s] = r : delete l2[s];
      }
    }, e2.emit = (s, i2, l2) => {
      let f2 = t2[s] || [], r = null;
      for (let u2 = 0, b = f2.length; u2 < b; u2++) {
        let v = f2[u2].call(i2, l2);
        r === null && v === false && (r = true);
      }
      return r;
    }, e2;
  }
  function S(t2, e2, s) {
    let i2 = e2.options.classes, l2 = e2.options.fade, f2 = g2({ easing: l2.easing, duration: l2.duration * 2 }), r = (n2) => {
      if (typeof n2 != "number")
        return e2.active !== t2.index && (e2.active = t2.index), t2;
      if (e2.folds.get(n2))
        return e2.active = t2.index, e2.folds.get(n2);
      throw new Error(`Relapse: No fold exists using id: "${n2}"`);
    }, u2 = () => e2.folds.filter(({ expanded: n2 }) => n2).length, b = (n2, o, a2) => {
      h(n2, "will-change", "opacity,visibility");
      let d = n2.animate({ opacity: o, visibility: a2 }, o[0] === "1" ? f2 : l2);
      d.onfinish = () => {
        h(n2, "opacity", o[1]), h(n2, "visibility", a2[1]), n2.style.removeProperty("will-change");
      };
    }, v = (n2, o, a2) => {
      e2.status = 2;
      let d = n2.animate(e2.semantic ? { height: o } : { maxHeight: o }, e2.options.fold);
      d.onfinish = a2;
    }, p2 = (n2) => {
      if (n2.disabled)
        return;
      let o = n2.button, a2 = n2.wrapper, d = n2.element;
      h(a2, "overflow", "hidden"), e2.semantic ? (n2.height = o.offsetHeight, h(a2, "will-change", "height")) : (n2.height = 0, h(a2, "will-change", "max-height"));
      let c = `${n2.height}px`;
      b(d, ["1", "0"], ["visible", "hidden"]), v(a2, [`${a2.offsetHeight}px`, c], () => {
        e2.semantic ? (h(a2, "height", c), a2.removeAttribute("open")) : h(d, "max-height", "0"), n2.expanded = false, w2(o, 2), A2(o, 2), R2(o, i2.opened), R2(a2, i2.expanded), a2.style.removeProperty("will-change"), e2.openCount = u2(), e2.status = 1, s.emit("collapse", e2, n2);
      });
    }, y = (n2) => (o) => {
      n2.close();
      let a2 = n2.button, d = n2.wrapper, c = n2.element;
      w2(a2), A2(a2);
      let x2;
      e2.semantic ? (d.setAttribute("open", ""), h(d, "will-change", "height"), h(d, "overflow", "hidden"), n2.height = a2.offsetHeight + c.offsetHeight, x2 = `${a2.offsetHeight}px`) : (h(d, "will-change", "max-height"), n2.height = c.scrollHeight, x2 = "0px"), L2(a2, i2.opened), b(c, ["0", "1"], ["hidden", "visible"]), v(d, [x2, `${n2.height}px`], () => {
        e2.semantic ? h(d, "height", "auto") : h(d, "max-height", "inherit"), d.style.removeProperty("overflow"), d.style.removeProperty("will-change"), L2(c, i2.expanded), n2.expanded = true, e2.openCount = u2(), e2.status = 1, e2.options.persist && e2.openCount > 1 && n2.disable(), s.emit("expand", e2, n2);
      });
    };
    t2.close = (n2) => {
      let o = r(n2);
      if (e2.options.multiple)
        (e2.options.persist === true && e2.openCount > 1 || e2.options.persist === false && o.expanded === true) && p2(o);
      else
        for (let a2 of e2.folds)
          if (a2.expanded === true && a2.locked === false) {
            if (e2.options.persist && a2.index === o.index)
              break;
            p2(a2), o = a2;
            break;
          }
      o.enable(), e2.openCount = u2();
    }, t2.focus = () => {
      e2.active = t2.index, t2.button.focus({ preventScroll: true }), s.emit("focus", e2, t2);
    }, t2.enable = (n2) => {
      let o = r(n2);
      o.disabled && o.locked === false && (o.disabled = false, w2(o.button, 2), R2(o.button, i2.disabled));
    }, t2.disable = (n2) => {
      let o = r(n2);
      o.disabled || (o.expanded ? e2.options.persist && (o.disabled = true, w2(o.button)) : (o.close(), o.disabled = true, w2(o.button), L2(o.button, i2.disabled)));
    }, t2.open = (n2) => {
      let o = r(n2);
      e2.status === 2 || o.expanded || requestAnimationFrame(y(o));
    }, t2.toggle = (n2) => {
      if (e2.semantic && n2.preventDefault(), !(e2.status === 2 || s.emit("toggle", e2, t2) === false))
        return t2.expanded ? t2.close() : t2.open();
    }, t2.destroy = () => {
      t2.button.removeEventListener("click", t2.toggle), t2.button.removeEventListener("focus", t2.focus), t2.button.removeEventListener("blur", t2.blur);
    }, t2.button.addEventListener("click", t2.toggle), t2.button.addEventListener("focus", t2.focus), e2.folds.push(t2), e2.folds.ref[t2.element.id] = e2.folds.length - 1;
  }
  function k2(t2) {
    return typeof t2 == "object" && "schema" in t2 ? t2.schema : "[data-relapse]";
  }
  function N2(t2) {
    let e2 = g2({ persist: false, unique: false, multiple: false, schema: "data-relapse", fold: g2({ duration: 200, easing: "ease-in-out" }), fade: g2({ duration: 120, easing: "linear" }), classes: g2({ opened: "opened", disabled: "disabled", expanded: "expanded" }) });
    if (typeof t2 == "object") {
      for (let s in t2)
        if (s === "classes" || s === "fade" || s === "fold")
          for (let i2 in t2[s])
            e2[s][i2] = t2[s][i2];
        else
          e2[s] = t2[s];
      "fade" in t2 && "duration" in t2.fade && t2.fade.duration !== e2.fade.duration || e2.fold.duration !== 200 && (e2.fade.duration = e2.fold.duration - e2.fold.duration / 2.5);
    }
    return e2;
  }
  function D2(t2, e2) {
    let s = t2.schema.length + 1, i2 = g2({ fold: g2(), fade: g2(), classes: g2() });
    for (let { nodeName: l2, nodeValue: f2 } of e2) {
      if (!l2.startsWith(t2.schema))
        continue;
      let r = l2.slice(s);
      if (r === "schema") {
        console.warn('Relapse: The "schema" option cannot be defined via attribute');
        continue;
      }
      let u2 = f2.trim(), b = `Invalid ${l2} attribute value.`;
      if (r === "persist" || r === "multiple" || r === "unique")
        if (u2 === "true" || u2 === "false")
          i2[r] = u2 === "true";
        else
          throw new TypeError(`Relapse: ${b}. Boolean expected, received: ${u2}`);
      else if (r === "fold-duration" || r === "fade-duration") {
        if (isNaN(+u2))
          throw new TypeError(`Relapse: ${b}. Number expected, received: ${u2}`);
        {
          let [v, p2] = r.split("-");
          i2[v][p2] = +u2;
        }
      } else if (r.startsWith("class-") || r === "fold-easing" || r === "fade-easing") {
        let [v, p2] = r.split("-");
        i2[v][p2] = u2;
      }
    }
    !("fade" in i2 && "duration" in i2.fade && i2.fade.duration !== t2.fade.duration) && "fold" in i2 && "duration" in i2.fold && i2.fold.duration !== t2.fold.duration && (i2.fade.duration = i2.fold.duration - i2.fold.duration / 2.5);
    for (let l2 in i2)
      if (l2 === "fold" || l2 === "classes" || l2 === "fade")
        for (let f2 in i2[l2])
          t2[l2][f2] = i2[l2][f2];
      else
        t2[l2] = i2[l2];
    return t2;
  }
  var E2 = function t(e2, s) {
    if (document.readyState === "loading") {
      addEventListener("DOMContentLoaded", () => t(e2, s));
      return;
    }
    let i2 = typeof e2 == "object" && "tagName" in e2;
    if (i2 && e2 instanceof NodeList)
      throw TypeError("Relapse: Invalid NodeList selector. Provide string or HTMLElement");
    if (i2) {
      let f2 = typeof e2 == "string" ? document.body.querySelector(e2) : e2;
      f2 !== null && M2(f2, N2(s));
    } else
      document.body.querySelectorAll(k2(e2)).forEach((f2) => M2(f2, N2(e2)));
    let l2 = C2(m2.values());
    return i2 ? l2[l2.length - 1] : l2;
  };
  E2.version = "0.8.1";
  E2.each = (t2) => {
    for (let [e2, s] of m2)
      if (t2(s, e2) === false)
        break;
  };
  E2.reinit = (t2) => {
    let e2 = E2.get(t2), s = T2(e2) ? e2 : [e2];
    for (let i2 of s) {
      let { id: l2, options: f2 } = i2;
      i2.destroy(), E2(`#${l2}`, f2);
    }
  };
  E2.has = (t2) => t2 ? (T2(t2) ? t2 : [t2]).every(m2.has) : m2.size > 0;
  E2.destroy = (t2) => {
    let e2 = E2.get(t2), s = T2(e2) ? e2 : [e2];
    for (let i2 of s)
      i2.destroy();
  };
  E2.get = (t2) => typeof t2 == "string" ? m2.has(t2) ? m2.get(t2) : null : T2(t2) ? t2.filter((e2) => m2.has(e2)).map((e2) => m2.get(e2)) : C2(m2.values());
  window.relapse || Object.defineProperty(window, "relapse", { get() {
    return E2;
  } });
  var I2 = E2;

  // node_modules/.pnpm/qvp@0.3.2/node_modules/qvp/index.js
  var i = function(t2) {
    if (Array.isArray(t2))
      return t2.forEach(i);
    if (!("id" in t2)) {
      for (let s of ["onenter", "onexit", "oninit", "onresize"])
        if (s in t2)
          throw new Error('qvp: Missing an "id" reference');
      return Object.entries(t2).forEach(([s, o]) => {
        let c = typeof t2[s];
        if (c === "string")
          return i({ id: s, query: o });
        throw new TypeError(`qvp: Invalid query type. Expected "string" received "${c}"`);
      });
    }
    let n2 = g3(t2), { id: r } = n2.screen;
    i.viewports.size || addEventListener("resize", u(() => {
      i.viewports.forEach((s) => {
        s.screen.active && s.onresize(window.innerWidth);
      });
    }, 25), true), i.viewports.has(r) ? console.warn(`qvp: The id "${r}" is already defined, use qvp.add() instead.`) : i.viewports.set(r, n2);
  };
  i.viewports = /* @__PURE__ */ new Map();
  Object.defineProperty(i, "isTouch", { get() {
    return "ontouchstart" in window || navigator.maxTouchPoints > 0 || navigator.msMaxTouchPoints > 0;
  } });
  i.get = (e2) => i.viewports.has(e2) ? i.viewports.get(e2) : false;
  i.add = (e2, t2) => {
    let n2 = i.get(e2);
    if (n2 === false)
      return console.error(`qvp: There is no viewport using an id of "${e2}"`);
    f(t2, n2.screen), n2.screen.test.matches && n2.onenter();
  };
  i.off = (e2, t2) => {
    let n2 = i.get(e2.split(":")[0]);
    if (n2 === false || !(e2 in n2.events))
      return;
    let r = n2.events[e2].length;
    if (typeof t2 == "number")
      t2 <= r - 1 && n2.events[e2].splice(t2, 1);
    else if (typeof t2 == "function") {
      let s = [];
      for (let o = 0; o < r; o++)
        n2.events[e2][o] !== t2 && s.push(n2.events[e2][o]);
      s.length > 0 && (n2.events[e2] = s);
    } else
      delete n2.events[e2];
  };
  i.on = (e2, t2, n2) => {
    let [r, s] = e2.split(":"), o = i.get(r);
    if (o === false)
      return;
    e2 in o.events || (o.events[e2] = []);
    let c = n2 ? t2.bind(n2) : t2;
    return c["qvp:event"] = o.events[e2].length, o.events[e2].push(c), o.screen.test.matches ? s === "oninit" ? c.call() : (s === "onenter" && c.call(), f({ [s]: c }, o.screen)) : f({ [s]: c }, o.screen), c["qvp:event"];
  };
  i.list = (e2) => {
    let t2 = Array.from(i.viewports.values()).map(({ screen: n2 }) => n2);
    return e2 ? t2.filter(({ id: n2 }) => e2.includes(n2)) : t2;
  };
  i.active = (e2) => {
    let t2 = i.list();
    if (e2) {
      let r = i.get(e2);
      return r ? r.screen.active : false;
    }
    let n2 = t2.filter(({ active: r }) => r === true);
    return n2.length > 1 ? n2 : n2[0];
  };
  i.test = (e2, t2 = ",") => typeof e2 == "string" ? e2.indexOf(t2) > -1 ? e2.split(t2).some(i.active) : !!i.active(e2) : e2.some(i.active);
  i.remove = (e2) => {
    i.viewports.has(e2) && (i.viewports.get(e2).destroy(), i.viewports.delete(e2));
  };
  i.destroy = () => {
    removeEventListener("resize", u()), i.viewports.forEach((e2) => e2.destroy()), i.viewports.clear();
  };
  i.screens = () => {
    throw Error("qvp: The qvp.screens() is deprecated, use the default import, e.g: qvp(...)");
  };
  function u(e2, t2) {
    let n2 = t2;
    return function() {
      let r = () => {
        n2 = null, e2.apply(this, arguments);
      };
      n2 && cancelAnimationFrame(n2), n2 = requestAnimationFrame(r);
    };
  }
  function f(e2, t2) {
    typeof e2.onenter == "function" && ("qvp:event" in e2.onenter || (e2.onenter["qvp:event"] = NaN), t2.onenter.add(e2.onenter)), typeof e2.onexit == "function" && ("qvp:event" in e2.onexit || (e2.onexit["qvp:event"] = NaN), t2.onexit.add(e2.onexit)), typeof e2.onresize == "function" && ("qvp:event" in e2.onresize || (e2.onresize["qvp:event"] = NaN), t2.onresize.add(e2.onresize)), typeof e2.oninit == "function" && ("qvp:event" in e2.oninit || (e2.oninit["qvp:event"] = NaN), t2.oninit.add(e2.oninit));
  }
  function a(e2, ...t2) {
    let n2 = i.get(e2.split(":")[0]);
    if (n2 !== false && e2 in n2.events)
      for (let r = 0; r < n2.events[e2].length && (n2.events[e2][r].apply(null, t2), e2.endsWith(":oninit") && i.off(e2, r), e2 in n2.events); r++)
        ;
  }
  function g3(e2) {
    let t2 = e2.query || "all", n2 = { id: e2.id, query: t2, active: false, test: matchMedia(t2), onenter: /* @__PURE__ */ new Set(), onexit: /* @__PURE__ */ new Set(), onresize: /* @__PURE__ */ new Set(), oninit: /* @__PURE__ */ new Set(), events: /* @__PURE__ */ Object.create(null) };
    f(e2, n2);
    let r = () => {
      n2.oninit.size > 0 && (n2.oninit.forEach((v) => !isNaN(v["qvp:event"]) || v()), a(`${e2.id}:oninit`), n2.oninit.clear()), n2.onenter.forEach((v) => !isNaN(v["qvp:event"]) || v()), a(`${e2.id}:onenter`), n2.active = true;
    }, s = () => {
      n2.onexit.forEach((v) => !isNaN(v["qvp:event"]) || v()), a(`${e2.id}:onexit`), n2.active = false;
    }, o = (v) => {
      n2.onresize.forEach((d) => !isNaN(d["qvp:event"]) || d()), a(`${e2.id}:onresize`, v);
    }, c = ({ matches: v }) => v ? r() : s(), p2 = () => {
      for (let v in n2.events)
        delete n2.events[v];
      n2.test.removeEventListener("change", c);
    };
    return n2.test.addEventListener("change", c), n2.test.matches && r(), { onenter: r, onexit: s, onresize: o, destroy: p2, get screen() {
      return n2;
    }, get events() {
      return n2.events;
    } };
  }
  var l = i;

  // src/app/components/drawer.ts
  var _Drawer = class _Drawer extends Ic.Component {
    constructor() {
      super(...arguments);
      this.transition = (event) => {
        if (event.propertyName !== "transform")
          return;
        if (this.state.hasShift) {
          for (const shift of this.shifts) {
            if (shift.classList.contains(this.shiftClass)) {
              shift.classList.remove(this.shiftClass);
              shift.style.removeProperty("transform");
            }
          }
        }
        if (this.backdrop.classList.contains("backdrop")) {
          this.backdrop.classList.remove("backdrop");
        }
        if (this.state.mode === "pull") {
          this.shifts.item(0).removeEventListener(event.type, this.transition);
        } else {
          this.target.removeEventListener(event.type, this.transition);
        }
      };
      /**
       * Click detected outside, eg: document body
       */
      this.outsideClick = (event) => {
        if (event.target !== this.target) {
          this.close();
          this.html.removeEventListener("click", this.outsideClick, false);
        }
      };
      /**
       * Toggle Drawer
       */
      this.toggle = (event) => {
        if (event)
          event.preventDefault();
        this.state.isOpen = !this.state.isOpen;
        if (this.state.isOpen) {
          this.open();
        } else {
          this.close();
        }
        return this.state.isOpen ? this.open() : this.close();
      };
      /**
       * Touch Move prevention event
       */
      this.touchMove = (event) => {
        if (this.state.isOpen) {
          if (this.target.scrollHeight <= this.target.clientHeight) {
            event.preventDefault();
          }
        }
      };
      /**
       * Keyboard events
       */
      this.keyboard = (event) => {
        switch (event.code) {
          case "Esc":
          case "Escape":
            this.close();
            break;
        }
      };
    }
    /**
     * Returns the backdrop element
     */
    get backdrop() {
      return _Drawer.backdrop;
    }
    /**
     * Returns the drawer direction class name
     */
    get directionClass() {
      return `drawer-${this.state.direction}`;
    }
    /**
     * Returns the drawer shift class name
     */
    get shiftClass() {
      return `drawer-${this.state.mode}`;
    }
    /**
     * Returns the shifts transition class name
     */
    get shifts() {
      return this.html.querySelectorAll(this.state.shift);
    }
    /**
     * Returns all button toggles in the dom
     */
    get buttons() {
      return this.html.querySelectorAll(`[data-drawer="${this.target.id}"]`);
    }
    /**
     * Stimulus: Initialize
     */
    oninit() {
      if (!_Drawer.backdrop) {
        _Drawer.backdrop = document.createElement("div");
        _Drawer.backdrop.className = "drawer-backdrop";
        _Drawer.backdrop.setAttribute("spx-morph", "false");
      }
      if (this.state.useParent) {
        this.target = this.dom.parentElement;
        this.target.ariaHidden = "true";
      } else {
        this.target = this.dom;
      }
      if (this.target.classList.contains("d-none")) {
        this.target.classList.remove("d-none");
      }
      if (this.state.mode !== "overlay" && this.state.hasShift === false) {
        console.error('Missing "data-drawer-shift-value" defintions on:', this.target);
      }
      if (this.html.contains(_Drawer.backdrop) === false) {
        this.html.appendChild(_Drawer.backdrop);
      }
      if (this.state.hasWidth) ;
      if (this.state.hasHeight) {
        this.target.style.setProperty("height", this.state.height);
      }
      if (this.state.hasDirection && this.target.classList.contains("backdrop") === false) {
        this.target.classList.add("backdrop");
      }
      if (this.state.mode === "pull") {
        this.target.style.setProperty("transform", "translateX(0)");
        this.target.style.setProperty("z-index", "0");
      }
      if (this.html.classList.contains("drawer-open")) {
        this.html.classList.remove("drawer-open");
      }
    }
    onload() {
      if (this.state.isOpen) {
        if (l.test(["lg", "xl", "xxl"])) {
          this.close();
        } else {
          setTimeout(() => this.close(), 250);
        }
      }
    }
    /**
     * Open Drawer
     */
    open() {
      if (!this.target.classList.contains("drawer-active")) {
        this.target.classList.add("drawer-active");
      }
      if (!this.backdrop.classList.contains("backdrop")) {
        this.backdrop.classList.add("backdrop");
      }
      if (this.state.bodyScroll === false) {
        this.html.style.setProperty("overflow", "hidden");
      }
      if (this.state.hasShift) {
        this.shiftElements();
      }
      if (this.state.width) {
        if (this.state.direction === "top") {
          this.backdrop.style.setProperty("transform", `translateY(-${this.state.offset})`);
        } else {
          this.backdrop.style.setProperty("transform", `translateX(${this.state.width})`);
        }
      }
      this.html.classList.add("drawer-open");
      this.backdrop.addEventListener("click", this.toggle, { once: true });
      this.target.addEventListener("touchstart", this.touchStart, { passive: true });
      this.target.ariaHidden = "false";
    }
    close() {
      if (this.state.isOpen) {
        this.state.isOpen = false;
      }
      if (this.state.width) {
        this.backdrop.style.removeProperty("transform");
      }
      if (this.state.bodyScroll === false) {
        this.html.style.removeProperty("overflow");
      }
      if (this.state.hasShift) {
        this.shiftElements();
      } else {
        this.target.addEventListener("transitionend", this.transition);
      }
      this.html.classList.remove("drawer-open");
      this.target.removeEventListener("touchstart", this.touchStart);
      this.backdrop.removeEventListener("click", this.toggle);
      this.target.classList.remove("drawer-active");
      this.target.ariaHidden = "true";
    }
    /**
     * Set attribute requirements for the elements which apply transform shifting
     */
    shiftElements() {
      if (this.state.mode === "pull") {
        this.target.style.setProperty("transform", "translateX(0)");
        this.target.style.setProperty("z-index", "0");
        if (this.state.isOpen === false) {
          this.shifts.item(0).addEventListener("transitionend", this.transition);
        }
      } else {
        if (this.state.isOpen === false) {
          this.target.addEventListener("transitionend", this.transition);
        }
      }
      for (const shift of this.shifts) {
        if (this.state.isOpen) {
          if (!shift.classList.contains(this.shiftClass)) {
            shift.classList.add(this.shiftClass);
          }
          if (this.state.width && (this.state.direction === "left" || this.state.direction === "right")) {
            shift.style.setProperty("transform", `translateX(${this.state.width})`);
          } else if (this.state.hasHeight && (this.state.direction === "top" || this.state.direction === "bottom")) {
            shift.style.setProperty("transform", `translateY(${this.state.height})`);
          }
        } else {
          if (this.state.width && (this.state.direction === "left" || this.state.direction === "right")) {
            shift.style.setProperty("transform", "translateX(0)");
          } else if (this.state.hasHeight && (this.state.direction === "top" || this.state.direction === "bottom")) {
            shift.style.setProperty("transform", "translateY(0)");
          }
        }
      }
    }
    /**
     * Touch Start scroll position
     */
    touchStart({ target }) {
      if (target instanceof HTMLElement) {
        const { scrollTop, offsetHeight } = target;
        const position = scrollTop + offsetHeight;
        if (scrollTop === 0) {
          target.scrollTop = 1;
        } else if (position === scrollTop) {
          target.scrollTop = scrollTop - 1;
        }
      }
    }
  };
  /**
   * Stimulus: values
   */
  _Drawer.connect = {
    state: {
      outsideClick: Boolean,
      height: String,
      width: String,
      offset: String,
      direction: String,
      shift: String,
      redraw: String,
      useParent: {
        typeof: Boolean,
        default: false
      },
      isOpen: {
        typeof: Boolean,
        default: false
      },
      bodyScroll: {
        typeof: Boolean,
        default: false
      },
      backdrop: {
        typeof: Boolean,
        default: true
      },
      mode: {
        typeof: String,
        default: "overlay"
      }
    }
  };
  var Drawer = _Drawer;

  // src/app/components/tabs.ts
  var Tabs = class extends Ic.Component {
    oninit() {
      this.state.size = this.tabNodes.length;
    }
    toggle({ attrs }) {
      if (this.state.open === attrs.index)
        return;
      for (let i2 = 0, s = this.state.size; i2 < s; i2++) {
        if (i2 === attrs.index) {
          this.buttonNodes[i2].classList.add("active");
          this.tabNodes[i2].classList.remove("d-none");
        } else {
          this.buttonNodes[i2].classList.remove("active");
          this.tabNodes[i2].classList.toggle("d-none", true);
        }
      }
      this.state.open = attrs.index;
    }
  };
  Tabs.connect = {
    state: {
      size: Number,
      open: {
        default: 0,
        typeof: Number
      }
    }
  };

  // src/app/components/methods.ts
  var Methods = class extends Ic.Component {
    onload() {
      this.methods = I2(this.relapseNode, {
        multiple: true,
        persist: false
      });
      this.methods.on("toggle", (fold) => {
        if (fold.expanded) {
          this.collapse({ attrs: { fold: fold.index } });
        } else {
          this.expand({ attrs: { fold: fold.index } });
        }
      });
    }
    expand({ attrs }) {
      this.methods.expand(attrs.fold);
      this.openerNodes[attrs.fold].classList.add("disabled");
      this.openerNodes[attrs.fold].classList.remove("active");
      this.closerNodes[attrs.fold].classList.remove("disabled");
      this.closerNodes[attrs.fold].classList.add("active");
    }
    collapse({ attrs }) {
      this.methods.collapse(attrs.fold);
      this.openerNodes[attrs.fold].classList.remove("disabled");
      this.closerNodes[attrs.fold].classList.remove("active");
      this.closerNodes[attrs.fold].classList.add("disabled");
    }
    disable({ attrs }) {
      this.methods.expand(attrs.fold);
    }
  };
  Methods.connect = {
    state: {
      open: {
        default: 0,
        typeof: Number
      }
    }
  };

  // src/app/components/events.ts
  var Events = class extends Ic.Component {
    constructor() {
      super(...arguments);
      this.colors = ["fc-cyan", "fc-pink", "fc-purple", "fc-green"];
    }
    onload() {
      this.relapse = I2(this.relapseNode);
      this.relapse.on("focus", this.focus, this);
      this.relapse.on("collapse", this.collapse, this);
      this.relapse.on("expand", this.expand, this);
      this.relapse.on("toggle", this.toggle, this);
    }
    insert(message, fold) {
      ++this.state.count;
      const element = document.createElement("div");
      element.className = `d-block pb-1 message ${this.colors[fold]}`;
      element.ariaLabel = `${this.state.count}`;
      element.innerText = message;
      this.logNode.appendChild(element);
      this.logNode.scrollTop = this.logNode.scrollHeight;
    }
    toggle(fold) {
      this.insert(
        `Fold at index ${fold.index} was toggled and will ${fold.expanded ? "collapse" : "expand"}`,
        fold.index
      );
    }
    focus(fold) {
      this.insert(
        `Fold at index ${fold.index} was focused and will ${fold.expanded ? "collapse" : "expand"}`,
        fold.index
      );
    }
    collapse(fold) {
      this.insert(
        `Fold at index ${fold.index} was collapsed and is closed`,
        fold.index
      );
    }
    expand(fold) {
      this.insert(
        `Fold at index ${fold.index} was expanded and is open`,
        fold.index
      );
    }
  };
  Events.connect = {
    state: {
      count: Number
    }
  };

  // src/app/components/dropdown.ts
  var Dropdown = class extends Ic.Component {
    /**
     * Returns all `<label>` elements in the dropdown
     */
    inViewport() {
      const rect = this.collapseNode.getBoundingClientRect();
      for (const { element, folds } of I2.get()) {
        if (element.id === this.state.accordion) {
          if (!(rect.top >= 0 && rect.left >= 0 && rect.bottom <= (window.innerHeight || document.documentElement.clientHeight) && rect.right <= (window.innerWidth || document.documentElement.clientWidth))) {
            folds.find((fold) => fold.expanded === true).close();
          }
          break;
        }
      }
    }
    /**
     * Toggle - Open/Close
     */
    toggle(event) {
      event.stopPropagation();
      if (this.dom.classList.contains("is-open"))
        return this.close();
      this.state.collapse = "opened";
      this.dom.classList.add("is-open");
      this.buttonNode.classList.remove("selected");
      if (this.state.hasAccordion)
        this.inViewport();
      addEventListener("click", this.outsideClick.bind(this));
    }
    /**
     * Click detected outside, eg: document body
     */
    outsideClick(event) {
      if (this.buttonNode !== event.target && this.collapseNode !== event.target) {
        if (this.dom.classList.contains("is-open")) {
          this.close();
        }
      }
    }
    /**
     * Close Dropdown
     */
    close() {
      this.dom.classList.remove("is-open");
      if (this.state.collapse === "selected" || this.state.hasSelected) {
        this.dom.classList.add("selected");
        this.state.collapse = "selected";
      } else {
        this.state.collapse = "closed";
      }
      removeEventListener("click", this.outsideClick);
      this.buttonNode.focus();
    }
    /**
     * Select Inputs
     *
     * Used for Dropdown Forms
     */
    select({ target }) {
      target.checked = true;
      this.state.selected = target.value;
      this.buttonNode.innerText = target.getAttribute("aria-label");
      this.state.collapse = "selected";
      for (const label of this.dom.getElementsByTagName("label")) {
        if (label.getAttribute("for") === target.id) {
          if (!label.classList.contains("selected")) {
            label.classList.add("selected");
          }
        } else {
          if (label.classList.contains("selected")) {
            label.classList.remove("selected");
          }
        }
      }
      this.close();
    }
    /**
     * Items in dropdown - An ul > li <select> element equivelent
     */
    option(event) {
      if (event.target instanceof HTMLElement) {
        if (event.currentTarget instanceof HTMLElement) {
          const [selected] = event.currentTarget.getElementsByClassName("selected");
          if (selected)
            this.state.selected = selected.id;
        }
        if (event.currentTarget instanceof HTMLElement) ;
        if (this.state.hasRequired) {
          if (this.buttonNode.classList.contains("is-invalid")) {
            this.buttonNode.classList.remove("is-invalid");
          }
          this.state.required = false;
          this.buttonNode.classList.add("selected");
        }
        if (this.state.kind === "preset") {
          this.state.selected = `Preset (${event.target.textContent.trim()})`;
          this.buttonNode.innerHTML = `Preset (${event.target.textContent.trim()})<span class="icon"></span>`;
        } else {
          this.state.selected = event.target.textContent;
          this.buttonNode.textContent = event.target.textContent;
        }
        for (const node of this.collapseNode.children) {
          if (node.id !== event.target.id) {
            node.classList.remove("selected");
          } else {
            node.classList.add("selected");
          }
        }
        this.state.collapse = "selected";
        this.toggle(event);
      }
    }
  };
  Dropdown.connect = {
    state: {
      selected: String,
      form: String,
      accordion: String,
      kind: String,
      required: {
        typeof: Boolean,
        default: false
      },
      collapse: {
        typeof: String,
        default: "closed"
      },
      type: {
        typeof: String,
        default: "dropdown"
      }
    }
  };

  // src/app/bundle.ts
  Ic.connect(
    {
      fragments: [
        "main",
        "menu"
      ],
      components: {
        Methods,
        Dropdown,
        Events,
        Tabs,
        Drawer
      }
    }
  )(function() {
    I2();
  });
  Ic.on("load", function({ key }) {
    I2.reinit();
  });

})();
